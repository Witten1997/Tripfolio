# Tripfolio Agent 约定

## 输出与行为约束

- 只输出完成当前明确请求所必需的内容，禁止无关说明、客套话、重复用户要求和代码总结。
- 除非用户明确要求，否则不写 README、文档说明、测试代码、使用说明或示例代码。
- 用户要求代码时直接给出所需代码；只需修改局部函数时，不输出整个文件。
- 不解释实现理由，不提供多个方案让用户选择，不询问是否还需要其他内容，直接采用最佳方案。
- 只做用户明确要求的事情，不擅自增加功能、过度优化或重构无关代码。
- 要求不清楚时，只询问一个最关键的问题。
- 输出应尽可能简短，避免无效 token 消耗。
