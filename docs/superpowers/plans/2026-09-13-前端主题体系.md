# 前端主题体系实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> 进度真相源仍是 `2026-09-12-web-p0.md` 的“切片 T”一节；本文件是每个任务的详细做法。本项目不由 AI 提交 git，任务末尾不含提交步骤，由用户决定何时提交。

**Goal:** 前端支持多套可切换的整体视觉主题，预置“有机自然”与“玻璃态”两套，用户在主题中心点击切换并持久化；新增主题只增文件与一条注册记录。

**Architecture:** 页面只引用项目语义令牌 `--tf-*`；两份桥接文件把令牌映射到 Element Plus 与 Vant 的变量；每套主题是 `[data-theme]` 下的令牌赋值加一份装饰样式；Pinia store 负责加载、应用与经平台层持久化；两个壳共用 store 与注册表。

**Tech Stack:** Vue 3.5、Pinia 4、Element Plus 2.14、Vant 4.10、Vite 8、Vitest 5（jsdom）、`@capacitor/preferences` 8.0.1（新增）。

**Spec:** `docs/architecture/2026-09-13-前端主题体系设计.md`

## Global Constraints

- 页面、壳、组件的 `<style>` 与模板属性中禁止字面颜色（`#hex`、`rgb(`）、禁止 `var(--el-*)` 与 `var(--van-*)`；只允许 `var(--tf-*)`。由 T.7 的样式契约测试强制。
- 颜色令牌只允许 `#rrggbb` 或 `rgb(r g b / a)` 两种写法。`text-1`、`text-2` 对 `canvas` 与合成后的 `surface` 对比度 ≥ 4.5，`text-3` ≥ 3，`accent-contrast` 对 `accent` ≥ 4.5。
- 全站配色禁止蓝紫色（色相 190–310 且饱和度 > 0.1），`themes.spec.ts` 对令牌、装饰层字面色与预览色强制校验；玻璃态是半透明白色玻璃（亮色），不是深色。
- 表示“无”的阴影类令牌一律写 `0 0 0 0 transparent`，不能写 `none`（会让多值 `box-shadow` 失效）。
- 主题 `decor.css` 只允许作用于：`.tf-backdrop`、`.tf-surface`、`.tf-card`、`.tf-bar`、`.el-card`、`.el-dialog`、`.el-drawer`、`.el-message-box`、`.van-cell-group--inset`、`.van-nav-bar`、`.van-popup`。
- 平台能力经 `src/platform` 接口访问；页面不直接使用 `localStorage`。
- 每个任务结束前运行 `pnpm --filter @tripfolio/client test` 与 `pnpm typecheck`；全部完成后跑 `pnpm build && pnpm format:check`。
- 本机不能构建 APK；安卓工程只做 `cap update android` 登记插件。

---

### Task T.1: 令牌契约与解析工具

**Files:**
- Create: `apps/client/src/shared/theme/tokens.ts`
- Test: `apps/client/src/shared/theme/tokens.spec.ts`

**Interfaces:**
- Produces: `REQUIRED_TOKENS: readonly string[]`、`COLOR_TOKENS: readonly TokenName[]`、`type TokenName`、`parseThemeTokens(css: string): Map<string, string>`、`parseColor(value: string): Rgba | null`、`contrastRatio(fg: Rgba, bg: Rgba): number`、`composite(fg: Rgba, bg: Rgba): Rgba`。

- [ ] **Step 1: 写失败的测试**

```ts
// apps/client/src/shared/theme/tokens.spec.ts
import { describe, expect, it } from 'vitest'

import {
  COLOR_TOKENS,
  REQUIRED_TOKENS,
  composite,
  contrastRatio,
  parseColor,
  parseThemeTokens,
} from './tokens'

describe('parseThemeTokens', () => {
  it('提取 --tf-* 声明并忽略注释与非 tf 变量', () => {
    const css = `
      /* 注释里的 --tf-fake: #000; 不算 */
      [data-theme='x'] {
        --tf-accent: #4f7a4a;
        --el-color-primary: red;
        --tf-radius-organic: 62% 38% 46% 54% / 50% 42% 58% 50%;
        --tf-backdrop: radial-gradient(circle at 20% 10%, #fff 0, transparent 60%), #f4efe6;
      }
    `
    const tokens = parseThemeTokens(css)
    expect([...tokens.keys()]).toEqual(['accent', 'radius-organic', 'backdrop'])
    expect(tokens.get('radius-organic')).toBe('62% 38% 46% 54% / 50% 42% 58% 50%')
  })
})

describe('parseColor', () => {
  it('支持 #rrggbb 与 rgb(r g b / a)', () => {
    expect(parseColor('#4f7a4a')).toEqual({ r: 79, g: 122, b: 74, a: 1 })
    expect(parseColor('rgb(43 42 36 / 0.45)')).toEqual({ r: 43, g: 42, b: 36, a: 0.45 })
    expect(parseColor('rgb(255 255 255 / 16%)')).toEqual({ r: 255, g: 255, b: 255, a: 0.16 })
  })
  it('拒绝其它写法', () => {
    expect(parseColor('#fff')).toBeNull()
    expect(parseColor('rgba(0,0,0,0.5)')).toBeNull()
    expect(parseColor('white')).toBeNull()
  })
})

describe('contrastRatio', () => {
  it('黑白为 21，同色为 1', () => {
    const black = parseColor('#000000')!
    const white = parseColor('#ffffff')!
    expect(contrastRatio(black, white)).toBeCloseTo(21, 1)
    expect(contrastRatio(white, white)).toBe(1)
  })
  it('半透明前景先与背景合成', () => {
    const fg = parseColor('rgb(255 255 255 / 0.5)')!
    const bg = parseColor('#000000')!
    expect(composite(fg, bg)).toEqual({ r: 128, g: 128, b: 128, a: 1 })
    expect(contrastRatio(fg, bg)).toBeGreaterThan(4)
    expect(contrastRatio(fg, bg)).toBeLessThan(21)
  })
})

describe('令牌清单', () => {
  it('颜色令牌都在必需清单内且无重复', () => {
    expect(new Set(REQUIRED_TOKENS).size).toBe(REQUIRED_TOKENS.length)
    for (const name of COLOR_TOKENS) expect(REQUIRED_TOKENS).toContain(name)
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @tripfolio/client exec vitest run src/shared/theme/tokens.spec.ts`
Expected: FAIL，模块 `./tokens` 不存在。

- [ ] **Step 3: 实现**

```ts
// apps/client/src/shared/theme/tokens.ts
/**
 * 主题令牌契约：页面与组件只允许引用 --tf-* 变量，每套主题必须给清单中的每个令牌赋值。
 * 清单与 docs/architecture/2026-09-13-前端主题体系设计.md 第 3 节保持一致。
 */
export const REQUIRED_TOKENS = [
  // 强调色与语义色
  'accent',
  'accent-contrast',
  'accent-soft',
  'success',
  'success-soft',
  'warning',
  'warning-soft',
  'danger',
  'danger-soft',
  'info',
  'info-soft',
  // 表面与文字
  'canvas',
  'surface',
  'surface-raised',
  'surface-sunken',
  'text-1',
  'text-2',
  'text-3',
  'text-disabled',
  'line',
  'line-soft',
  'overlay',
  // 形状与表面效果
  'radius-control',
  'radius-card',
  'radius-panel',
  'radius-organic',
  'surface-blur',
  'surface-border',
  'surface-highlight',
  // 光影与背景
  'shadow-1',
  'shadow-2',
  'shadow-3',
  'backdrop',
  // 动效
  'ease',
  'duration-fast',
  'duration',
  // 图表色板（切片 4 接入 ECharts 时读取）
  'chart-1',
  'chart-2',
  'chart-3',
  'chart-4',
  'chart-5',
  'chart-6',
] as const

export type TokenName = (typeof REQUIRED_TOKENS)[number]

/** 必须是可解析颜色的令牌（用于格式与对比度校验）。 */
export const COLOR_TOKENS: readonly TokenName[] = [
  'accent',
  'accent-contrast',
  'accent-soft',
  'success',
  'success-soft',
  'warning',
  'warning-soft',
  'danger',
  'danger-soft',
  'info',
  'info-soft',
  'canvas',
  'surface',
  'surface-raised',
  'surface-sunken',
  'text-1',
  'text-2',
  'text-3',
  'text-disabled',
  'line',
  'line-soft',
  'overlay',
  'chart-1',
  'chart-2',
  'chart-3',
  'chart-4',
  'chart-5',
  'chart-6',
]

export interface Rgba {
  r: number
  g: number
  b: number
  a: number
}

const COMMENT = /\/\*[\s\S]*?\*\//g
const DECLARATION = /--tf-([a-z0-9-]+)\s*:\s*([^;]+);/g
const HEX = /^#([0-9a-f]{6})$/i
const RGB = /^rgb\(\s*(\d{1,3})\s+(\d{1,3})\s+(\d{1,3})\s*(?:\/\s*([0-9.]+%?)\s*)?\)$/i

/** 从一份主题 CSS 中提取 --tf-* 声明，键不含前缀。 */
export function parseThemeTokens(css: string): Map<string, string> {
  const tokens = new Map<string, string>()
  for (const match of css.replace(COMMENT, '').matchAll(DECLARATION)) {
    tokens.set(match[1]!, match[2]!.trim())
  }
  return tokens
}

/** 只接受 #rrggbb 与 rgb(r g b / a) 两种写法（设计文档第 3 节）。 */
export function parseColor(value: string): Rgba | null {
  const text = value.trim()
  const hex = HEX.exec(text)
  if (hex) {
    const n = Number.parseInt(hex[1]!, 16)
    return { r: (n >> 16) & 255, g: (n >> 8) & 255, b: n & 255, a: 1 }
  }
  const rgb = RGB.exec(text)
  if (!rgb) return null
  const alphaText = rgb[4]
  const a =
    alphaText === undefined
      ? 1
      : alphaText.endsWith('%')
        ? Number(alphaText.slice(0, -1)) / 100
        : Number(alphaText)
  return { r: Number(rgb[1]), g: Number(rgb[2]), b: Number(rgb[3]), a }
}

/** 把带透明度的前景色合成到背景上（背景视为不透明）。 */
export function composite(fg: Rgba, bg: Rgba): Rgba {
  const mix = (f: number, b: number) => Math.round(f * fg.a + b * (1 - fg.a))
  return { r: mix(fg.r, bg.r), g: mix(fg.g, bg.g), b: mix(fg.b, bg.b), a: 1 }
}

function channel(value: number): number {
  const s = value / 255
  return s <= 0.03928 ? s / 12.92 : ((s + 0.055) / 1.055) ** 2.4
}

function luminance(color: Rgba): number {
  return 0.2126 * channel(color.r) + 0.7152 * channel(color.g) + 0.0722 * channel(color.b)
}

/** WCAG 对比度；前景若半透明先合成。 */
export function contrastRatio(fg: Rgba, bg: Rgba): number {
  const l1 = luminance(composite(fg, bg))
  const l2 = luminance(bg)
  const [hi, lo] = l1 > l2 ? [l1, l2] : [l2, l1]
  return (hi + 0.05) / (lo + 0.05)
}
```

- [ ] **Step 4: 运行确认通过**

Run: `pnpm --filter @tripfolio/client exec vitest run src/shared/theme/tokens.spec.ts`
Expected: PASS（7 个用例）。

---

### Task T.2: 主题注册表与两套主题的令牌文件

**Files:**
- Create: `apps/client/src/shared/theme/registry.ts`
- Create: `apps/client/src/themes/organic/tokens.css`、`apps/client/src/themes/organic/decor.css`（先空）、`apps/client/src/themes/organic/index.css`
- Create: `apps/client/src/themes/glass/tokens.css`、`apps/client/src/themes/glass/decor.css`（先空）、`apps/client/src/themes/glass/index.css`
- Test: `apps/client/src/shared/theme/themes.spec.ts`

**Interfaces:**
- Consumes: T.1 的 `REQUIRED_TOKENS`、`COLOR_TOKENS`、`parseThemeTokens`、`parseColor`、`contrastRatio`、`composite`。
- Produces: `themeIds`、`type ThemeId`、`interface ThemeDefinition { id; name; description; colorScheme: 'light' | 'dark'; preview: { canvas; surface; accent }; load(): Promise<unknown> }`、`DEFAULT_THEME`、`themes`、`isThemeId(value: unknown): value is ThemeId`、`findTheme(id: ThemeId): ThemeDefinition`。

- [ ] **Step 1: 写失败的测试**

```ts
// apps/client/src/shared/theme/themes.spec.ts
import { describe, expect, it } from 'vitest'

import { DEFAULT_THEME, findTheme, isThemeId, themeIds, themes } from './registry'
import {
  COLOR_TOKENS,
  REQUIRED_TOKENS,
  composite,
  contrastRatio,
  parseColor,
  parseThemeTokens,
} from './tokens'

const files = import.meta.glob<string>('../../themes/*/tokens.css', {
  query: '?raw',
  import: 'default',
  eager: true,
})

function tokensOf(id: string) {
  const css = files[`../../themes/${id}/tokens.css`]
  if (css === undefined) throw new Error(`缺少 src/themes/${id}/tokens.css`)
  return { css, tokens: parseThemeTokens(css) }
}

describe('主题注册表', () => {
  it('每个 themeIds 都有定义，默认主题存在', () => {
    expect(themes.map((t) => t.id)).toEqual([...themeIds])
    expect(findTheme(DEFAULT_THEME).id).toBe(DEFAULT_THEME)
    expect(isThemeId('glass')).toBe(true)
    expect(isThemeId('neon')).toBe(false)
    expect(isThemeId(null)).toBe(false)
  })
  it('预览色可解析', () => {
    for (const theme of themes) {
      for (const value of Object.values(theme.preview)) expect(parseColor(value)).not.toBeNull()
    }
  })
})

describe.each(themeIds)('主题 %s 的令牌文件', (id) => {
  it('只在自己的 data-theme 选择器下赋值', () => {
    const { css } = tokensOf(id)
    expect(css).toContain(`[data-theme='${id}']`)
    for (const other of themeIds) if (other !== id) expect(css).not.toContain(`'${other}'`)
  })
  it('定义的令牌集合与 REQUIRED_TOKENS 完全一致', () => {
    const { tokens } = tokensOf(id)
    expect([...tokens.keys()].sort()).toEqual([...REQUIRED_TOKENS].sort())
  })
  it('颜色令牌格式合法', () => {
    const { tokens } = tokensOf(id)
    for (const name of COLOR_TOKENS) {
      expect(parseColor(tokens.get(name)!), `--tf-${name}`).not.toBeNull()
    }
  })
  it('阴影类令牌不写 none', () => {
    const { tokens } = tokensOf(id)
    for (const name of ['shadow-1', 'shadow-2', 'shadow-3', 'surface-highlight']) {
      expect(tokens.get(name), `--tf-${name}`).not.toBe('none')
    }
  })
  it('文字与强调色对比度达标', () => {
    const { tokens } = tokensOf(id)
    const color = (name: string) => parseColor(tokens.get(name)!)!
    const canvas = color('canvas')
    const surface = composite(color('surface'), canvas)
    for (const bg of [canvas, surface]) {
      expect(contrastRatio(color('text-1'), bg)).toBeGreaterThanOrEqual(4.5)
      expect(contrastRatio(color('text-2'), bg)).toBeGreaterThanOrEqual(4.5)
      expect(contrastRatio(color('text-3'), bg)).toBeGreaterThanOrEqual(3)
    }
    expect(contrastRatio(color('accent-contrast'), color('accent'))).toBeGreaterThanOrEqual(4.5)
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @tripfolio/client exec vitest run src/shared/theme/themes.spec.ts`
Expected: FAIL，`./registry` 不存在。

- [ ] **Step 3: 写注册表**

```ts
// apps/client/src/shared/theme/registry.ts
/**
 * 主题注册表：新增主题只需新建 src/themes/<id>/ 三个文件并在此登记一条。
 * load 用动态 import，让每套主题的 CSS 单独成 chunk，首屏只加载当前主题。
 */
export const themeIds = ['organic', 'glass'] as const

export type ThemeId = (typeof themeIds)[number]

export interface ThemeDefinition {
  id: ThemeId
  name: string
  description: string
  /** 决定 color-scheme 与 Vant 的暗色类。 */
  colorScheme: 'light' | 'dark'
  /** 主题中心的色块与 <meta name="theme-color">。 */
  preview: { canvas: string; surface: string; accent: string }
  load: () => Promise<unknown>
}

export const DEFAULT_THEME: ThemeId = 'organic'

export const themes: readonly ThemeDefinition[] = [
  {
    id: 'organic',
    name: '有机自然',
    description: '流动的形状、自然的色调与柔和的曲线。',
    colorScheme: 'light',
    preview: { canvas: '#f4efe6', surface: '#fffdf8', accent: '#4f7a4a' },
    load: () => import('@/themes/organic/index.css'),
  },
  {
    id: 'glass',
    name: '玻璃态',
    description: '半透明的白色玻璃、模糊的背景与通透的光感。',
    colorScheme: 'light',
    preview: { canvas: '#e9eee9', surface: '#ffffff', accent: '#1f7a6d' },
    load: () => import('@/themes/glass/index.css'),
  },
]

export function isThemeId(value: unknown): value is ThemeId {
  return typeof value === 'string' && (themeIds as readonly string[]).includes(value)
}

export function findTheme(id: ThemeId): ThemeDefinition {
  const theme = themes.find((t) => t.id === id)
  if (!theme) throw new Error(`未注册的主题：${id}`)
  return theme
}
```

- [ ] **Step 4: 写有机自然主题令牌**

```css
/* apps/client/src/themes/organic/tokens.css */
[data-theme='organic'] {
  --tf-accent: #4f7a4a;
  --tf-accent-contrast: #ffffff;
  --tf-accent-soft: #e2ebdc;
  --tf-success: #3f8a5a;
  --tf-success-soft: #e0efe5;
  --tf-warning: #b8782a;
  --tf-warning-soft: #f6e9d3;
  --tf-danger: #c4553d;
  --tf-danger-soft: #f7dfd8;
  --tf-info: #5c7f8f;
  --tf-info-soft: #dfeaef;

  --tf-canvas: #f4efe6;
  --tf-surface: #fffdf8;
  --tf-surface-raised: #ffffff;
  --tf-surface-sunken: #ece5d8;
  --tf-text-1: #2b2a24;
  --tf-text-2: #5d5a50;
  --tf-text-3: #7d7868;
  --tf-text-disabled: #b8b2a4;
  --tf-line: #d9d1c2;
  --tf-line-soft: #e8e1d3;
  --tf-overlay: rgb(43 42 36 / 0.45);

  --tf-radius-control: 12px;
  --tf-radius-card: 24px;
  --tf-radius-panel: 28px;
  --tf-radius-organic: 62% 38% 46% 54% / 50% 42% 58% 50%;
  --tf-surface-blur: 0px;
  --tf-surface-border: 1px solid #e8e1d3;
  --tf-surface-highlight: 0 0 0 0 transparent;

  --tf-shadow-1: 0 1px 2px rgb(60 50 30 / 0.06);
  --tf-shadow-2: 0 10px 28px -14px rgb(60 50 30 / 0.22);
  --tf-shadow-3: 0 28px 56px -20px rgb(60 50 30 / 0.3);
  --tf-backdrop: #f4efe6;

  --tf-ease: cubic-bezier(0.22, 1, 0.36, 1);
  --tf-duration-fast: 150ms;
  --tf-duration: 320ms;

  --tf-chart-1: #4f7a4a;
  --tf-chart-2: #c4855a;
  --tf-chart-3: #7a9cb0;
  --tf-chart-4: #b7a24a;
  --tf-chart-5: #8d6a9f;
  --tf-chart-6: #5f9e8a;
}
```

- [ ] **Step 5: 写玻璃态主题令牌**

见 `apps/client/src/themes/glass/tokens.css`（2026-09-13 按用户要求由深色蓝紫改为亮色白玻璃，青绿主色 `#1f7a6d`，半透明白表面）。

- [ ] **Step 6: 写两个 index.css 与空 decor.css**

```css
/* apps/client/src/themes/organic/index.css 与 glass/index.css 内容相同 */
@import './tokens.css';
@import './decor.css';
```

`decor.css` 先只放一行注释 `/* 装饰层在 T.9 填充 */`，避免 Prettier 报空文件。

- [ ] **Step 7: 运行确认通过**

Run: `pnpm --filter @tripfolio/client exec vitest run src/shared/theme/`
Expected: PASS。若 `?raw` 读取失败，检查 glob 相对路径是否以 spec 文件所在目录为基准。

---

### Task T.3: 基础样式与组件库桥接

**Files:**
- Create: `apps/client/src/styles/base.css`（并入并删除 `src/style.css`）
- Create: `apps/client/src/styles/bridge/element-plus.css`、`apps/client/src/styles/bridge/vant.css`
- Modify: `apps/client/src/main.ts:8`（`./style.css` → `./styles/base.css`）
- Modify: `apps/client/src/desktop/DesktopShell.vue:4`（在 `element-plus/dist/index.css` 之后引入桥接）
- Modify: `apps/client/src/mobile/MobileShell.vue:3`（在 `vant/lib/index.css` 之后引入桥接）
- Test: `apps/client/src/shared/theme/bridge.spec.ts`

**Interfaces:**
- Consumes: T.1 `REQUIRED_TOKENS`。
- Produces: 钩子类 `.tf-backdrop`、`.tf-surface`、`.tf-card`、`.tf-bar`、`.tf-theme-switching`；令牌 `--tf-font-body`（跨主题常量，定义在 base.css 的 `:root`）。

- [ ] **Step 1: 写失败的测试（桥接只能引用契约内的令牌）**

```ts
// apps/client/src/shared/theme/bridge.spec.ts
import { describe, expect, it } from 'vitest'

import { REQUIRED_TOKENS } from './tokens'

const files = import.meta.glob<string>('../../styles/**/*.css', {
  query: '?raw',
  import: 'default',
  eager: true,
})

/** base.css 自己定义的跨主题常量，桥接与页面也可以引用。 */
const BASE_TOKENS = ['font-body', 'font-display']

describe('桥接与基础样式', () => {
  it('存在两份桥接与 base.css', () => {
    expect(Object.keys(files).sort()).toEqual([
      '../../styles/base.css',
      '../../styles/bridge/element-plus.css',
      '../../styles/bridge/vant.css',
    ])
  })
  it.each(Object.keys(files))('%s 只引用契约内的 --tf-* 令牌', (path) => {
    const allowed = new Set<string>([...REQUIRED_TOKENS, ...BASE_TOKENS])
    const used = [...files[path]!.matchAll(/var\(--tf-([a-z0-9-]+)/g)].map((m) => m[1]!)
    expect(used.length).toBeGreaterThan(0)
    for (const name of used) expect(allowed.has(name), `--tf-${name}`).toBe(true)
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @tripfolio/client exec vitest run src/shared/theme/bridge.spec.ts`
Expected: FAIL，`files` 为空对象。

- [ ] **Step 3: 写 base.css（内容取代原 style.css）**

```css
/* apps/client/src/styles/base.css：跨主题不变的基础规则与钩子类。 */
:root {
  --tf-font-body:
    system-ui, -apple-system, 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC',
    sans-serif;
  --tf-font-display: var(--tf-font-body);
  font-family: var(--tf-font-body);
}

html,
body,
#app {
  margin: 0;
  min-height: 100%;
}

body {
  -webkit-font-smoothing: antialiased;
  padding: env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom)
    env(safe-area-inset-left);
  background: var(--tf-canvas);
  color: var(--tf-text-1);
  transition: background-color var(--tf-duration) var(--tf-ease);
}

/* 壳层固定背景：主题 decor.css 在其上叠加装饰。 */
.tf-backdrop {
  position: fixed;
  inset: 0;
  z-index: -1;
  overflow: hidden;
  pointer-events: none;
  background: var(--tf-backdrop);
}

/* 页面自建面板（非组件库卡片）。 */
.tf-surface {
  background: var(--tf-surface);
  border: var(--tf-surface-border);
  border-radius: var(--tf-radius-card);
  box-shadow: var(--tf-shadow-1), var(--tf-surface-highlight);
}

/* 顶栏与导航栏。 */
.tf-bar {
  background: var(--tf-surface-raised);
  border-bottom: 1px solid var(--tf-line-soft);
}

/* 切换主题的一帧内抑制所有过渡，避免颜色拖影。 */
.tf-theme-switching,
.tf-theme-switching *,
.tf-theme-switching *::before,
.tf-theme-switching *::after {
  transition: none !important;
  animation: none !important;
}

@media (prefers-reduced-motion: reduce) {
  :root {
    --tf-duration-fast: 0ms;
    --tf-duration: 0ms;
  }
}
```

- [ ] **Step 4: 写 Element Plus 桥接**

```css
/* apps/client/src/styles/bridge/element-plus.css：把 --tf-* 映射到 Element Plus 变量，只写一次。
   必须在 element-plus/dist/index.css 之后引入。淡色档向 canvas 混合、深色档向 text-1 混合，
   亮暗主题都只需提供一个主色。 */
:root {
  --el-color-primary: var(--tf-accent);
  --el-color-primary-light-3: color-mix(in oklab, var(--tf-accent) 70%, var(--tf-canvas));
  --el-color-primary-light-5: color-mix(in oklab, var(--tf-accent) 50%, var(--tf-canvas));
  --el-color-primary-light-7: color-mix(in oklab, var(--tf-accent) 30%, var(--tf-canvas));
  --el-color-primary-light-8: color-mix(in oklab, var(--tf-accent) 20%, var(--tf-canvas));
  --el-color-primary-light-9: color-mix(in oklab, var(--tf-accent) 10%, var(--tf-canvas));
  --el-color-primary-dark-2: color-mix(in oklab, var(--tf-accent) 80%, var(--tf-text-1));

  --el-color-success: var(--tf-success);
  --el-color-success-light-3: color-mix(in oklab, var(--tf-success) 70%, var(--tf-canvas));
  --el-color-success-light-5: color-mix(in oklab, var(--tf-success) 50%, var(--tf-canvas));
  --el-color-success-light-7: color-mix(in oklab, var(--tf-success) 30%, var(--tf-canvas));
  --el-color-success-light-8: color-mix(in oklab, var(--tf-success) 20%, var(--tf-canvas));
  --el-color-success-light-9: color-mix(in oklab, var(--tf-success) 10%, var(--tf-canvas));
  --el-color-success-dark-2: color-mix(in oklab, var(--tf-success) 80%, var(--tf-text-1));

  --el-color-warning: var(--tf-warning);
  --el-color-warning-light-3: color-mix(in oklab, var(--tf-warning) 70%, var(--tf-canvas));
  --el-color-warning-light-5: color-mix(in oklab, var(--tf-warning) 50%, var(--tf-canvas));
  --el-color-warning-light-7: color-mix(in oklab, var(--tf-warning) 30%, var(--tf-canvas));
  --el-color-warning-light-8: color-mix(in oklab, var(--tf-warning) 20%, var(--tf-canvas));
  --el-color-warning-light-9: color-mix(in oklab, var(--tf-warning) 10%, var(--tf-canvas));
  --el-color-warning-dark-2: color-mix(in oklab, var(--tf-warning) 80%, var(--tf-text-1));

  --el-color-danger: var(--tf-danger);
  --el-color-danger-light-3: color-mix(in oklab, var(--tf-danger) 70%, var(--tf-canvas));
  --el-color-danger-light-5: color-mix(in oklab, var(--tf-danger) 50%, var(--tf-canvas));
  --el-color-danger-light-7: color-mix(in oklab, var(--tf-danger) 30%, var(--tf-canvas));
  --el-color-danger-light-8: color-mix(in oklab, var(--tf-danger) 20%, var(--tf-canvas));
  --el-color-danger-light-9: color-mix(in oklab, var(--tf-danger) 10%, var(--tf-canvas));
  --el-color-danger-dark-2: color-mix(in oklab, var(--tf-danger) 80%, var(--tf-text-1));

  --el-color-error: var(--tf-danger);
  --el-color-error-light-3: var(--el-color-danger-light-3);
  --el-color-error-light-5: var(--el-color-danger-light-5);
  --el-color-error-light-7: var(--el-color-danger-light-7);
  --el-color-error-light-8: var(--el-color-danger-light-8);
  --el-color-error-light-9: var(--el-color-danger-light-9);
  --el-color-error-dark-2: var(--el-color-danger-dark-2);

  --el-color-info: var(--tf-info);
  --el-color-info-light-3: color-mix(in oklab, var(--tf-info) 70%, var(--tf-canvas));
  --el-color-info-light-5: color-mix(in oklab, var(--tf-info) 50%, var(--tf-canvas));
  --el-color-info-light-7: color-mix(in oklab, var(--tf-info) 30%, var(--tf-canvas));
  --el-color-info-light-8: color-mix(in oklab, var(--tf-info) 20%, var(--tf-canvas));
  --el-color-info-light-9: color-mix(in oklab, var(--tf-info) 10%, var(--tf-canvas));
  --el-color-info-dark-2: color-mix(in oklab, var(--tf-info) 80%, var(--tf-text-1));

  --el-bg-color: var(--tf-surface-raised);
  --el-bg-color-page: var(--tf-canvas);
  --el-bg-color-overlay: var(--tf-surface-raised);
  --el-text-color-primary: var(--tf-text-1);
  --el-text-color-regular: var(--tf-text-2);
  --el-text-color-secondary: var(--tf-text-3);
  --el-text-color-placeholder: var(--tf-text-3);
  --el-text-color-disabled: var(--tf-text-disabled);
  --el-border-color: var(--tf-line);
  --el-border-color-light: var(--tf-line-soft);
  --el-border-color-lighter: var(--tf-line-soft);
  --el-border-color-extra-light: var(--tf-line-soft);
  --el-border-color-dark: var(--tf-line);
  --el-border-color-darker: var(--tf-line);
  --el-fill-color: var(--tf-surface-sunken);
  --el-fill-color-light: var(--tf-surface-sunken);
  --el-fill-color-lighter: var(--tf-surface-sunken);
  --el-fill-color-extra-light: var(--tf-surface-sunken);
  --el-fill-color-dark: var(--tf-line-soft);
  --el-fill-color-darker: var(--tf-line);
  --el-fill-color-blank: var(--tf-surface-sunken);
  --el-mask-color: var(--tf-overlay);
  --el-mask-color-extra-light: var(--tf-overlay);
  --el-overlay-color: var(--tf-overlay);
  --el-overlay-color-light: var(--tf-overlay);
  --el-overlay-color-lighter: var(--tf-overlay);

  --el-box-shadow: var(--tf-shadow-2);
  --el-box-shadow-light: var(--tf-shadow-1);
  --el-box-shadow-lighter: var(--tf-shadow-1);
  --el-box-shadow-dark: var(--tf-shadow-3);
  --el-border-radius-base: var(--tf-radius-control);
  --el-border-radius-small: calc(var(--tf-radius-control) / 2);
  --el-font-family: var(--tf-font-body);
  --el-transition-duration: var(--tf-duration);
  --el-transition-duration-fast: var(--tf-duration-fast);
  --el-transition-function-ease-in-out-bezier: var(--tf-ease);
  --el-transition-function-fast-bezier: var(--tf-ease);

  --el-card-bg-color: var(--tf-surface);
  --el-card-border-color: var(--tf-line-soft);
  --el-card-border-radius: var(--tf-radius-card);
  --el-dialog-bg-color: var(--tf-surface-raised);
  --el-dialog-border-radius: var(--tf-radius-panel);
  --el-dialog-box-shadow: var(--tf-shadow-3);
  --el-messagebox-border-radius: var(--tf-radius-panel);
  --el-drawer-bg-color: var(--tf-surface-raised);
}
```

- [ ] **Step 5: 写 Vant 桥接**

```css
/* apps/client/src/styles/bridge/vant.css：把 --tf-* 映射到 Vant 变量，只写一次。
   必须在 vant/lib/index.css 之后引入。同时覆盖 .van-theme-dark，保证暗色主题下仍以令牌为准。 */
:root,
.van-theme-dark {
  --van-primary-color: var(--tf-accent);
  --van-success-color: var(--tf-success);
  --van-danger-color: var(--tf-danger);
  --van-warning-color: var(--tf-warning);
  --van-text-color: var(--tf-text-1);
  --van-text-color-2: var(--tf-text-2);
  --van-text-color-3: var(--tf-text-3);
  --van-background: var(--tf-canvas);
  --van-background-2: var(--tf-surface);
  --van-background-3: var(--tf-surface-raised);
  --van-border-color: var(--tf-line-soft);
  --van-active-color: var(--tf-surface-sunken);
  --van-radius-sm: calc(var(--tf-radius-control) / 2);
  --van-radius-md: var(--tf-radius-control);
  --van-radius-lg: var(--tf-radius-card);
  --van-duration-base: var(--tf-duration);
  --van-duration-fast: var(--tf-duration-fast);
  --van-notice-bar-text-color: var(--tf-danger);
  --van-notice-bar-background: var(--tf-danger-soft);
}
```

- [ ] **Step 6: 接线**

`main.ts` 第 8 行改为 `import './styles/base.css'`，删除 `src/style.css`。
`DesktopShell.vue` 的 `import 'element-plus/dist/index.css'` 后一行加 `import '@/styles/bridge/element-plus.css'`。
`MobileShell.vue` 的 `import 'vant/lib/index.css'` 后一行加 `import '@/styles/bridge/vant.css'`。

- [ ] **Step 7: 运行确认通过**

Run: `pnpm --filter @tripfolio/client exec vitest run src/shared/theme/ && pnpm --filter @tripfolio/client typecheck`
Expected: PASS。

---

### Task T.4: 平台层偏好存储

**Files:**
- Modify: `apps/client/src/platform/types.ts`（新增 `KeyValueStore`，`Platform` 加 `preferences`）
- Create: `apps/client/src/platform/web/preferences.ts`、`apps/client/src/platform/capacitor/preferences.ts`
- Modify: `apps/client/src/platform/index.ts`
- Modify: `apps/client/package.json`（新增 `@capacitor/preferences: 8.0.1`）与 `pnpm-lock.yaml`
- Modify: `apps/client/android/capacitor.settings.gradle`、`apps/client/android/app/capacitor.build.gradle`（由 `cap update` 生成）
- Test: `apps/client/src/platform/web/preferences.spec.ts`

**Interfaces:**
- Produces: `interface KeyValueStore { get(key: string): Promise<string | null>; set(key: string, value: string): Promise<void>; remove(key: string): Promise<void> }`；`platform.preferences: KeyValueStore`。

- [ ] **Step 1: 安装依赖并登记安卓插件**

```bash
pnpm --filter @tripfolio/client add @capacitor/preferences@8.0.1
pnpm --filter @tripfolio/client exec cap update android
git -C D:/Code/Tripfolio status --short apps/client/android
```
Expected: 两个 gradle 文件新增 `capacitor-preferences` 条目；不要运行 `cap sync`（需要 dist）。

- [ ] **Step 2: 写失败的测试**

```ts
// apps/client/src/platform/web/preferences.spec.ts
import { beforeEach, describe, expect, it } from 'vitest'

import { webPreferences } from './preferences'

describe('webPreferences', () => {
  beforeEach(() => window.localStorage.clear())

  it('写入后可读出，删除后为 null', async () => {
    expect(await webPreferences.get('k')).toBeNull()
    await webPreferences.set('k', 'v')
    expect(await webPreferences.get('k')).toBe('v')
    await webPreferences.remove('k')
    expect(await webPreferences.get('k')).toBeNull()
  })
})
```

- [ ] **Step 3: 运行确认失败**

Run: `pnpm --filter @tripfolio/client exec vitest run src/platform/web/preferences.spec.ts`
Expected: FAIL，模块不存在。

- [ ] **Step 4: 实现**

`types.ts` 在 `SecureStorage` 前加：

```ts
/** 非敏感的本地偏好（主题等）。网页用 localStorage，安卓用 Preferences 插件。 */
export interface KeyValueStore {
  get(key: string): Promise<string | null>
  set(key: string, value: string): Promise<void>
  remove(key: string): Promise<void>
}
```

`Platform` 接口在 `network` 后加 `preferences: KeyValueStore`。

```ts
// apps/client/src/platform/web/preferences.ts
import type { KeyValueStore } from '../types'

export const webPreferences: KeyValueStore = {
  async get(key) {
    return window.localStorage.getItem(key)
  },
  async set(key, value) {
    window.localStorage.setItem(key, value)
  },
  async remove(key) {
    window.localStorage.removeItem(key)
  },
}
```

```ts
// apps/client/src/platform/capacitor/preferences.ts
import { Preferences } from '@capacitor/preferences'

import type { KeyValueStore } from '../types'

/** 安卓：应用私有 SharedPreferences；不加密，只放主题等非敏感偏好。 */
export const capacitorPreferences: KeyValueStore = {
  async get(key) {
    const { value } = await Preferences.get({ key })
    return value ?? null
  },
  async set(key, value) {
    await Preferences.set({ key, value })
  },
  async remove(key) {
    await Preferences.remove({ key })
  },
}
```

`index.ts` 引入两者，`createPlatform` 返回对象加 `preferences: isNative ? capacitorPreferences : webPreferences,`。

- [ ] **Step 5: 运行确认通过**

Run: `pnpm --filter @tripfolio/client exec vitest run src/platform/ && pnpm --filter @tripfolio/client typecheck`
Expected: PASS。

---

### Task T.5: DOM 应用函数与主题 store

**Files:**
- Create: `apps/client/src/shared/theme/dom.ts`
- Create: `apps/client/src/shared/stores/theme.ts`
- Test: `apps/client/src/shared/theme/dom.spec.ts`、`apps/client/src/shared/stores/theme.spec.ts`

**Interfaces:**
- Consumes: T.2 `ThemeDefinition`、`findTheme`、`isThemeId`、`DEFAULT_THEME`；T.4 `platform.preferences`。
- Produces: `THEME_ATTRIBUTE = 'data-theme'`、`SWITCHING_CLASS = 'tf-theme-switching'`、`applyThemeToDocument(doc: Document, theme: ThemeDefinition): void`；`THEME_PREFERENCE_KEY = 'tripfolio.theme'`、`useThemeStore()` 返回 `{ current: Ref<ThemeId>, ready: Ref<boolean>, definition: ComputedRef<ThemeDefinition>, restore(): Promise<void>, apply(id: ThemeId): Promise<void> }`。

- [ ] **Step 1: 写失败的测试**

```ts
// apps/client/src/shared/theme/dom.spec.ts
import { describe, expect, it } from 'vitest'

import { SWITCHING_CLASS, THEME_ATTRIBUTE, applyThemeToDocument } from './dom'
import { findTheme } from './registry'

const nextFrame = () => new Promise((resolve) => setTimeout(resolve, 50))

describe('applyThemeToDocument', () => {
  it('设置 data-theme、color-scheme、Vant 暗色类与 theme-color', async () => {
    const glass = findTheme('glass')
    applyThemeToDocument(document, glass)
    const root = document.documentElement
    expect(root.getAttribute(THEME_ATTRIBUTE)).toBe('glass')
    expect(root.style.colorScheme).toBe('dark')
    expect(root.classList.contains('van-theme-dark')).toBe(true)
    expect(root.classList.contains(SWITCHING_CLASS)).toBe(true)
    expect(document.querySelector('meta[name="theme-color"]')?.getAttribute('content')).toBe(
      glass.preview.canvas,
    )
    await nextFrame()
    expect(root.classList.contains(SWITCHING_CLASS)).toBe(false)
  })

  it('切回亮色主题时移除暗色类并更新 theme-color，不重复创建 meta', () => {
    applyThemeToDocument(document, findTheme('glass'))
    applyThemeToDocument(document, findTheme('organic'))
    const root = document.documentElement
    expect(root.getAttribute(THEME_ATTRIBUTE)).toBe('organic')
    expect(root.style.colorScheme).toBe('light')
    expect(root.classList.contains('van-theme-dark')).toBe(false)
    expect(document.querySelectorAll('meta[name="theme-color"]')).toHaveLength(1)
  })
})
```

```ts
// apps/client/src/shared/stores/theme.spec.ts
import { createPinia, setActivePinia } from 'pinia'
import { beforeEach, describe, expect, it, vi } from 'vitest'

import { THEME_PREFERENCE_KEY, useThemeStore } from './theme'

const store = vi.hoisted(() => new Map<string, string>())
vi.mock('@/platform', () => ({
  platform: {
    kind: 'web',
    isNative: false,
    preferences: {
      get: async (key: string) => store.get(key) ?? null,
      set: async (key: string, value: string) => void store.set(key, value),
      remove: async (key: string) => void store.delete(key),
    },
  },
}))

beforeEach(() => {
  setActivePinia(createPinia())
  store.clear()
  document.documentElement.removeAttribute('data-theme')
})

describe('useThemeStore', () => {
  it('没有持久化值时恢复为默认主题', async () => {
    const theme = useThemeStore()
    await theme.restore()
    expect(theme.current).toBe('organic')
    expect(theme.ready).toBe(true)
    expect(document.documentElement.dataset.theme).toBe('organic')
  })

  it('持久化值合法时恢复该主题，非法时回退默认', async () => {
    store.set(THEME_PREFERENCE_KEY, 'glass')
    const theme = useThemeStore()
    await theme.restore()
    expect(theme.current).toBe('glass')

    store.set(THEME_PREFERENCE_KEY, 'neon')
    setActivePinia(createPinia())
    const again = useThemeStore()
    await again.restore()
    expect(again.current).toBe('organic')
  })

  it('apply 切换并持久化，重复应用同一主题不重复写入', async () => {
    const theme = useThemeStore()
    await theme.restore()
    await theme.apply('glass')
    expect(theme.current).toBe('glass')
    expect(theme.definition.name).toBe('玻璃态')
    expect(store.get(THEME_PREFERENCE_KEY)).toBe('glass')
    expect(document.documentElement.dataset.theme).toBe('glass')

    store.delete(THEME_PREFERENCE_KEY)
    await theme.apply('glass')
    expect(store.has(THEME_PREFERENCE_KEY)).toBe(false)
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @tripfolio/client exec vitest run src/shared/theme/dom.spec.ts src/shared/stores/theme.spec.ts`
Expected: FAIL，模块不存在。

- [ ] **Step 3: 实现 dom.ts**

```ts
// apps/client/src/shared/theme/dom.ts
import type { ThemeDefinition } from './registry'

export const THEME_ATTRIBUTE = 'data-theme'
/** 切换瞬间加在 <html> 上，base.css 借此抑制所有过渡一帧。 */
export const SWITCHING_CLASS = 'tf-theme-switching'
const VANT_DARK_CLASS = 'van-theme-dark'

/** 把主题应用到文档：纯 DOM 操作，不含加载与持久化。 */
export function applyThemeToDocument(doc: Document, theme: ThemeDefinition): void {
  const root = doc.documentElement
  root.classList.add(SWITCHING_CLASS)
  root.setAttribute(THEME_ATTRIBUTE, theme.id)
  root.style.colorScheme = theme.colorScheme
  root.classList.toggle(VANT_DARK_CLASS, theme.colorScheme === 'dark')

  let meta = doc.querySelector<HTMLMetaElement>('meta[name="theme-color"]')
  if (!meta) {
    meta = doc.createElement('meta')
    meta.name = 'theme-color'
    doc.head.appendChild(meta)
  }
  meta.content = theme.preview.canvas

  // 强制回流让抑制类生效，下一帧再移除
  void root.offsetWidth
  const view = doc.defaultView
  const schedule =
    view?.requestAnimationFrame?.bind(view) ??
    ((callback: FrameRequestCallback) => setTimeout(() => callback(0), 16))
  schedule(() => root.classList.remove(SWITCHING_CLASS))
}
```

- [ ] **Step 4: 实现 store**

```ts
// apps/client/src/shared/stores/theme.ts
import { defineStore } from 'pinia'
import { computed, ref } from 'vue'

import { platform } from '@/platform'
import { applyThemeToDocument } from '@/shared/theme/dom'
import { DEFAULT_THEME, findTheme, isThemeId, type ThemeId } from '@/shared/theme/registry'

export const THEME_PREFERENCE_KEY = 'tripfolio.theme'

/**
 * 主题是本地偏好，不进入账号数据。restore 在应用挂载前调用一次；
 * apply 由主题中心调用，加载主题 CSS、应用到文档并持久化。
 */
export const useThemeStore = defineStore('theme', () => {
  const current = ref<ThemeId>(DEFAULT_THEME)
  const ready = ref(false)
  const definition = computed(() => findTheme(current.value))

  async function activate(id: ThemeId) {
    const theme = findTheme(id)
    await theme.load()
    applyThemeToDocument(document, theme)
    current.value = id
  }

  async function restore() {
    let saved: string | null = null
    try {
      saved = await platform.preferences.get(THEME_PREFERENCE_KEY)
    } catch {
      saved = null
    }
    await activate(isThemeId(saved) ? saved : DEFAULT_THEME)
    ready.value = true
  }

  async function apply(id: ThemeId) {
    if (ready.value && id === current.value) return
    await activate(id)
    await platform.preferences.set(THEME_PREFERENCE_KEY, id)
  }

  return { current, ready, definition, restore, apply }
})
```

- [ ] **Step 5: 运行确认通过**

Run: `pnpm --filter @tripfolio/client exec vitest run src/shared/ && pnpm --filter @tripfolio/client typecheck`
Expected: PASS。若 vitest 对 `import('@/themes/.../index.css')` 报错，在 `vitest.config.ts` 的 `test` 加 `css: { include: [] }` 之后再试；仍失败则在 store 测试里 `vi.spyOn(findTheme('glass'), 'load').mockResolvedValue(undefined)`。

---

### Task T.6: 启动装配

**Files:**
- Modify: `apps/client/src/main.ts`
- Modify: `apps/client/index.html:6`（`meta color-scheme` 改为由主题控制，保留 viewport）

- [ ] **Step 1: 改 main.ts**

```ts
import { createPinia } from 'pinia'
import { createApp } from 'vue'

import App from './App.vue'
import { platform } from './platform'
import { createAppRouter } from './router'
import { useThemeStore } from './shared/stores/theme'
import { pickShell } from './shell/pickShell'
import './styles/base.css'

// 启动时一次性决定壳：Capacitor 原生环境或窄视口用移动壳，其余用桌面壳。
const shell = pickShell({ isNative: platform.isNative, viewportWidth: window.innerWidth })

async function bootstrap() {
  const app = createApp(App, { shell })
  const pinia = createPinia()
  app.use(pinia)
  // 主题在挂载前恢复，首屏直接以正确主题渲染，避免闪烁
  await useThemeStore(pinia).restore()
  app.use(createAppRouter(shell))
  app.mount('#app')
}

void bootstrap()
```

- [ ] **Step 2: 改 index.html**

删除 `<meta name="color-scheme" content="light dark" />` 一行（由 `applyThemeToDocument` 设置 `color-scheme` 与 `theme-color`）。

- [ ] **Step 3: 验证**

Run: `pnpm --filter @tripfolio/client typecheck && pnpm --filter @tripfolio/client build`
Expected: 通过；`dist/assets` 中出现两个独立的主题 CSS chunk。

---

### Task T.7: 页面样式迁移与样式契约测试

**Files:**
- Modify: `apps/client/src/desktop/DesktopShell.vue`、`apps/client/src/mobile/MobileShell.vue`
- Modify: `apps/client/src/desktop/pages/TripListPage.vue`、`RecycleBinPage.vue`、`AccountPage.vue`、`auth.css`
- Modify: `apps/client/src/desktop/components/TripEditorDialog.vue`、`CategoryManagerDialog.vue`
- Modify: `apps/client/src/mobile/pages/TripListPage.vue`、`DevLocalDbPage.vue`
- Test: `apps/client/src/shared/theme/styleContract.spec.ts`

- [ ] **Step 1: 写失败的样式契约测试**

```ts
// apps/client/src/shared/theme/styleContract.spec.ts
import { describe, expect, it } from 'vitest'

/** 页面、壳与组件的样式只能引用 --tf-* 令牌；违反时在这里失败，而不是在视觉审查时才发现。 */
const files = import.meta.glob<string>('../../{desktop,mobile}/**/*.{vue,css}', {
  query: '?raw',
  import: 'default',
  eager: true,
})

const STYLE_BLOCK = /<style[^>]*>([\s\S]*?)<\/style>/g
const FORBIDDEN: Array<[RegExp, string]> = [
  [/#(?:[0-9a-f]{3}|[0-9a-f]{6}|[0-9a-f]{8})\b/i, '字面十六进制颜色'],
  [/\brgba?\(/i, '字面 rgb 颜色'],
  [/var\(--el-/, 'Element Plus 变量'],
  [/var\(--van-/, 'Vant 变量'],
]

function styleSources(path: string, source: string): string[] {
  if (path.endsWith('.css')) return [source]
  return [...source.matchAll(STYLE_BLOCK)].map((m) => m[1]!)
}

describe('样式契约', () => {
  it('至少扫描到页面文件', () => {
    expect(Object.keys(files).length).toBeGreaterThan(5)
  })

  it.each(Object.keys(files))('%s 的样式只引用 --tf-* 令牌', (path) => {
    const source = files[path]!
    for (const css of styleSources(path, source)) {
      for (const [pattern, label] of FORBIDDEN) {
        expect(pattern.test(css), `${label}：${pattern.exec(css)?.[0]}`).toBe(false)
      }
    }
    // 模板内联颜色属性（如 Vant NoticeBar 的 color/background）同样禁止
    expect(/\b(?:color|background)="#/.test(source), '模板内联字面颜色').toBe(false)
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @tripfolio/client exec vitest run src/shared/theme/styleContract.spec.ts`
Expected: FAIL，列出 TripListPage、DesktopShell 等使用 `var(--el-` 的文件与移动页的内联颜色。

- [ ] **Step 3: 迁移壳**

`DesktopShell.vue` 模板：`ElHeader` 加 `class="desktop-shell__header tf-bar"`；`ElContainer` 之前插入 `<div class="tf-backdrop" aria-hidden="true"></div>`（放在 `ElConfigProvider` 内、`ElContainer` 之上）；导航在 `v-if` 的 `nav` 之外增加一个始终显示的 `<RouterLink :to="{ name: 'themes' }" class="desktop-shell__theme">主题</RouterLink>`（放在 nav 与账号之间，结构如下）：

```html
<ElHeader class="desktop-shell__header tf-bar">
  <RouterLink to="/trips" class="desktop-shell__brand">Tripfolio</RouterLink>
  <nav class="desktop-shell__nav">
    <template v-if="session.isAuthenticated">
      <RouterLink :to="{ name: 'trips' }">旅行</RouterLink>
      <RouterLink :to="{ name: 'recycle-bin' }">回收站</RouterLink>
    </template>
    <RouterLink :to="{ name: 'themes' }">主题</RouterLink>
    <RouterLink v-if="session.isAuthenticated" :to="{ name: 'account' }" class="desktop-shell__account">
      {{ session.account?.nickname ?? '账号' }}
    </RouterLink>
  </nav>
</ElHeader>
```

样式：`.desktop-shell__header` 去掉 `border-bottom`（由 `.tf-bar` 提供），加 `position: sticky; top: 0; z-index: 10;`；`.desktop-shell__brand` 颜色 `var(--tf-text-1)`；`.desktop-shell__nav a` 颜色 `var(--tf-text-2)`，加 `transition: color var(--tf-duration-fast) var(--tf-ease)`；`.router-link-active` 颜色 `var(--tf-accent)`。

`MobileShell.vue`：`VanNavBar` 前插入 `<div class="tf-backdrop" aria-hidden="true"></div>`；`.mobile-shell` 的 `background: var(--van-background)` 改为 `background: transparent`（body 已有 canvas）。

- [ ] **Step 4: 迁移桌面页面与对话框**

逐文件把 `var(--el-text-color-primary)` → `var(--tf-text-1)`，`var(--el-text-color-regular)` → `var(--tf-text-2)`，`var(--el-text-color-secondary)` → `var(--tf-text-3)`，`var(--el-border-color-lighter)`/`var(--el-border-color)` → `var(--tf-line-soft)`/`var(--tf-line)`，`var(--el-color-primary)` → `var(--tf-accent)`，`var(--el-color-primary-light-9)` → `var(--tf-accent-soft)`，`var(--el-color-warning-light-9)` → `var(--tf-warning-soft)`，`var(--el-color-warning-light-5)` → `var(--tf-warning)`，`var(--el-fill-color-extra-light)` → `var(--tf-surface-sunken)`，`var(--el-bg-color)` → `var(--tf-surface)`，字面 `border-radius: 8px` → `var(--tf-radius-control)`。

具体点：
- `TripListPage.vue`：`.list-skeleton` 背景与圆角；`.trip-budget` 上边框；`ElCard` 加 `class="trip-card tf-card"`；`.trip-card` 增加 `transition: transform var(--tf-duration) var(--tf-ease), box-shadow var(--tf-duration) var(--tf-ease);` 与 `.trip-card:hover { transform: translateY(-2px); box-shadow: var(--tf-shadow-2); }`。
- `TripEditorDialog.vue`：`.conflict-panel` 背景 `var(--tf-warning-soft)`、边框 `1px solid var(--tf-warning)`、圆角 `var(--tf-radius-control)`；表格边框 `var(--tf-line-soft)`。
- `CategoryManagerDialog.vue`：`.category-row.selected` 背景 `var(--tf-accent-soft)`；`.category-editor` 背景 `var(--tf-surface-sunken)`、边框 `var(--tf-surface-border)`、圆角 `var(--tf-radius-control)`。
- `auth.css`：链接颜色 `var(--tf-accent)`。
- `AccountPage.vue`、`RecycleBinPage.vue`：文字色替换。

- [ ] **Step 5: 迁移移动页面**

`mobile/pages/TripListPage.vue` 与 `DevLocalDbPage.vue` 的 `VanNoticeBar` 删除 `color="#d03050"` 与 `background="#fdecef"` 两个属性（颜色由桥接的 `--van-notice-bar-*` 提供）。

- [ ] **Step 6: 运行确认通过**

Run: `pnpm --filter @tripfolio/client test && pnpm --filter @tripfolio/client typecheck`
Expected: 全部 PASS（含此前 89 个用例）。

---

### Task T.8: 主题中心页与入口

**Files:**
- Create: `apps/client/src/desktop/pages/ThemesPage.vue`、`apps/client/src/mobile/pages/ThemesPage.vue`
- Modify: `apps/client/src/router/routes.ts`（`pages.themes`、路由 `/themes`）
- Modify: `apps/client/src/mobile/pages/TripListPage.vue`（加“主题中心”单元格）
- Test: `apps/client/src/router/routes.spec.ts`

**Interfaces:**
- Consumes: T.2 `themes`、`ThemeId`；T.5 `useThemeStore`。

- [ ] **Step 1: 写失败的路由测试**

```ts
// apps/client/src/router/routes.spec.ts
import { describe, expect, it } from 'vitest'

import { buildRoutes } from './routes'

describe('buildRoutes', () => {
  it.each(['desktop', 'mobile'] as const)('%s 壳提供公开的主题中心路由', (shell) => {
    const route = buildRoutes(shell).find((r) => r.name === 'themes')
    expect(route?.path).toBe('/themes')
    expect(route?.meta?.public).toBe(true)
    expect(route?.meta?.guestOnly).toBeUndefined()
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm --filter @tripfolio/client exec vitest run src/router/routes.spec.ts`
Expected: FAIL，`route` 为 undefined。

- [ ] **Step 3: 路由**

`routes.ts` 的 `pages` 加：

```ts
  themes: {
    desktop: () => import('@/desktop/pages/ThemesPage.vue'),
    mobile: () => import('@/mobile/pages/ThemesPage.vue'),
  },
```

`buildRoutes` 在 `recycle-bin` 之后加：

```ts
    { path: '/themes', name: 'themes', component: pick(pages.themes), meta: { public: true } },
```

- [ ] **Step 4: 桌面主题中心**

```vue
<!-- apps/client/src/desktop/pages/ThemesPage.vue -->
<script setup lang="ts">
import { ElButton, ElTag } from 'element-plus'
import { ref } from 'vue'

import { useThemeStore } from '@/shared/stores/theme'
import { themes, type ThemeId } from '@/shared/theme/registry'

const theme = useThemeStore()
const switching = ref<ThemeId | null>(null)

async function choose(id: ThemeId) {
  if (switching.value) return
  switching.value = id
  try {
    await theme.apply(id)
  } finally {
    switching.value = null
  }
}
</script>

<template>
  <div class="themes-page">
    <header class="page-heading">
      <div>
        <h1>主题中心</h1>
        <p>选择一套界面风格，网页与手机端都会记住你的选择。</p>
      </div>
    </header>
    <div class="theme-grid">
      <article
        v-for="item in themes"
        :key="item.id"
        class="theme-card tf-surface"
        :class="{ active: item.id === theme.current }"
      >
        <div
          class="theme-preview"
          :style="{ background: item.preview.canvas }"
          aria-hidden="true"
        >
          <div class="theme-preview__card" :style="{ background: item.preview.surface }">
            <span class="theme-preview__accent" :style="{ background: item.preview.accent }"></span>
            <span class="theme-preview__line"></span>
            <span class="theme-preview__line short"></span>
          </div>
        </div>
        <div class="theme-copy">
          <div class="theme-title">
            <h2>{{ item.name }}</h2>
            <ElTag v-if="item.id === theme.current" type="success" size="small">当前使用</ElTag>
          </div>
          <p>{{ item.description }}</p>
        </div>
        <ElButton
          type="primary"
          :plain="item.id !== theme.current"
          :disabled="item.id === theme.current"
          :loading="switching === item.id"
          @click="choose(item.id)"
        >
          {{ item.id === theme.current ? '使用中' : '使用此主题' }}
        </ElButton>
      </article>
    </div>
  </div>
</template>

<style scoped>
.themes-page {
  max-width: 1240px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 20px;
}
.page-heading {
  padding: 8px 0;
}
.page-heading h1 {
  margin: 0;
  font-size: 28px;
  color: var(--tf-text-1);
}
.page-heading p {
  margin: 8px 0 0;
  color: var(--tf-text-3);
}
.theme-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 20px;
}
.theme-card {
  display: flex;
  flex-direction: column;
  gap: 16px;
  padding: 20px;
  transition:
    transform var(--tf-duration) var(--tf-ease),
    box-shadow var(--tf-duration) var(--tf-ease);
}
.theme-card:hover {
  transform: translateY(-2px);
  box-shadow: var(--tf-shadow-2), var(--tf-surface-highlight);
}
.theme-card.active {
  outline: 2px solid var(--tf-accent);
  outline-offset: 2px;
}
.theme-preview {
  height: 150px;
  border-radius: var(--tf-radius-control);
  padding: 22px;
  box-sizing: border-box;
}
.theme-preview__card {
  height: 100%;
  border-radius: var(--tf-radius-control);
  padding: 14px;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.theme-preview__accent {
  width: 44px;
  height: 12px;
  border-radius: 999px;
}
.theme-preview__line {
  height: 8px;
  width: 70%;
  border-radius: 999px;
  background: var(--tf-line);
}
.theme-preview__line.short {
  width: 45%;
}
.theme-title {
  display: flex;
  align-items: center;
  gap: 10px;
}
.theme-title h2 {
  margin: 0;
  font-size: 18px;
}
.theme-copy p {
  margin: 6px 0 0;
  color: var(--tf-text-2);
  line-height: 1.6;
}
</style>
```

- [ ] **Step 5: 移动主题中心与入口**

```vue
<!-- apps/client/src/mobile/pages/ThemesPage.vue -->
<script setup lang="ts">
import { Cell as VanCell, CellGroup as VanCellGroup, Icon as VanIcon } from 'vant'

import { useThemeStore } from '@/shared/stores/theme'
import { themes, type ThemeId } from '@/shared/theme/registry'

const theme = useThemeStore()

function choose(id: ThemeId) {
  void theme.apply(id)
}
</script>

<template>
  <VanCellGroup inset title="主题中心">
    <VanCell
      v-for="item in themes"
      :key="item.id"
      :title="item.name"
      :label="item.description"
      clickable
      @click="choose(item.id)"
    >
      <template #icon>
        <span class="theme-swatch" aria-hidden="true">
          <i :style="{ background: item.preview.canvas }"></i>
          <i :style="{ background: item.preview.surface }"></i>
          <i :style="{ background: item.preview.accent }"></i>
        </span>
      </template>
      <template #right-icon>
        <VanIcon v-if="item.id === theme.current" name="success" class="theme-check" />
      </template>
    </VanCell>
  </VanCellGroup>
</template>

<style scoped>
.theme-swatch {
  display: inline-flex;
  gap: 4px;
  margin-right: 12px;
  align-self: center;
}
.theme-swatch i {
  width: 14px;
  height: 14px;
  border-radius: 50%;
  border: 1px solid var(--tf-line);
}
.theme-check {
  color: var(--tf-accent);
  font-size: 18px;
  align-self: center;
}
</style>
```

`mobile/pages/TripListPage.vue` 在“开发工具”分组之前加：

```html
  <VanCellGroup inset title="外观">
    <VanCell title="主题中心" is-link :to="{ name: 'themes' }" />
  </VanCellGroup>
```

- [ ] **Step 6: 运行确认通过**

Run: `pnpm --filter @tripfolio/client test && pnpm --filter @tripfolio/client typecheck`
Expected: PASS。

---

### Task T.9: 两套主题的装饰层与视觉打磨

**Files:**
- Modify: `apps/client/src/themes/organic/decor.css`、`apps/client/src/themes/glass/decor.css`

- [ ] **Step 1: 有机自然装饰层**

```css
/* apps/client/src/themes/organic/decor.css：只作用于设计文档第 5 节的表面清单。 */
[data-theme='organic'] .tf-backdrop::before,
[data-theme='organic'] .tf-backdrop::after {
  content: '';
  position: absolute;
  border-radius: var(--tf-radius-organic);
  filter: blur(48px);
  opacity: 0.6;
}
[data-theme='organic'] .tf-backdrop::before {
  width: 42vw;
  height: 42vw;
  top: -14vw;
  right: -10vw;
  background: #dfe8d3;
}
[data-theme='organic'] .tf-backdrop::after {
  width: 36vw;
  height: 36vw;
  bottom: -16vw;
  left: -12vw;
  background: #f1dcc9;
}

[data-theme='organic'] .el-card,
[data-theme='organic'] .tf-card,
[data-theme='organic'] .tf-surface,
[data-theme='organic'] .van-cell-group--inset {
  border-radius: var(--tf-radius-card);
  box-shadow: var(--tf-shadow-1);
}
[data-theme='organic'] .el-card:nth-child(odd),
[data-theme='organic'] .tf-card:nth-child(odd) {
  border-top-left-radius: calc(var(--tf-radius-card) * 1.6);
}
[data-theme='organic'] .el-card:nth-child(even),
[data-theme='organic'] .tf-card:nth-child(even) {
  border-bottom-right-radius: calc(var(--tf-radius-card) * 1.6);
}
[data-theme='organic'] .el-dialog,
[data-theme='organic'] .el-message-box,
[data-theme='organic'] .van-popup {
  border-radius: var(--tf-radius-panel);
  box-shadow: var(--tf-shadow-3);
}
[data-theme='organic'] .el-button {
  border-radius: 999px;
}
```

装饰层允许字面颜色（只有这里允许），因为背景色团是主题本身的组成部分。

- [ ] **Step 2: 玻璃态装饰层**

见 `apps/client/src/themes/glass/decor.css`：薄荷与米色光斑、白色半透明玻璃片（顶部高光、细白边、底部暗一线、背景模糊 20px、斜向白色渐变）、`prefers-reduced-transparency` 回退。

- [ ] **Step 3: 浏览器验证**

启动 `go run ./cmd/api`（apps/server）与 `pnpm client:dev`，用 chrome-devtools MCP 打开 `http://localhost:5173/themes`，分别切换两套主题后截图旅行列表、新建旅行对话框、回收站、账号页、登录页与 1000px 以下窄视口，检查：文字可读、对话框未被裁切、下拉与日期选择器弹层在玻璃态下可读、切换时无颜色拖影、刷新页面后主题保持。移动壳（视口 ≤ 移动断点）检查导航栏与单元格组。

- [ ] **Step 4: 用 better-ui 与 better-colors 审查**

调用 `better-ui` 与 `better-colors` skill 审查 `src/themes/**`、`src/styles/**`、`ThemesPage.vue`，把 HIGH 与 MEDIUM 项修掉，LOW 记入进度日志。

---

### Task T.10: 收尾

- [ ] **Step 1: 全量验证**

```bash
cd D:/Code/Tripfolio && pnpm typecheck && pnpm test && pnpm build && pnpm format:check
```
不通过时先 `pnpm format`，再修剩余问题。

- [ ] **Step 2: 文档与日志**

- README 的前端说明加一句“主题体系见 docs/architecture/2026-09-13-前端主题体系设计.md，新增主题的步骤见该文第 7 节”。
- `2026-09-12-web-p0.md`：切片 T 各任务改 `[x]`，进度日志追加一行（新增用例数、验证结果、遗留：ECharts 主题接入放切片 4、账号级同步未做）。
- 建议用户以“切片 T”提交 git，不自行提交。
