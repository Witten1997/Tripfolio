# 网页版 P0 实施计划与进度

> **接手须知（任何会话开始前先读这一节）**
>
> - 本文件是唯一的进度真相源。任务状态：`[ ]` 待办、`[~]` 执行中、`[x]` 已完成、`[!]` 受阻（后面写原因）。
> - 开始一个任务前把它改成 `[~]`，完成并通过验证后改成 `[x]`，同时在文末“进度日志”追加一行（日期、任务号、一句话结果、遗留问题）。
> - 一次只允许一个 `[~]`。接手时若发现有 `[~]`，先按该任务的“验证”命令核对现状，再决定是继续还是回退。
> - 每个切片完成后建议用户提交一次 git（不要自行提交），提交信息写切片号；这样进度也能从 git log 恢复。
> - 环境：PostgreSQL 在 Docker 容器 `tripfolio-postgres`；集成测试需要 `TRIPFOLIO_TEST_DATABASE_URL=postgres://tripfolio:tripfolio@localhost:5432/tripfolio_test?sslmode=disable`（每条 Bash 命令都要显式带上，环境变量不跨命令保留）；sqlc 二进制在仓库根 `.tools/sqlc.exe`，缺失时 `bash apps/server/scripts/sqlc-install.sh`。
>
> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在已验证的工程骨架上实现网页版 P0：后端全部 REST 接口（含 2026-09-14 新增算路，共 89 个中除同步推拉与快照外的 80 个）与 23 张表，桌面壳全部页面；安卓与同步接口后置，但每次可同步写入都按设计写入变更日志，保证后续接入同步时无需改业务代码。

**Architecture:** 按切片纵向推进，每个切片交付“契约 → 生成 → 模块（含单元测试）→ PostgreSQL 适配（sqlc）→ HTTP 处理器 → 集成测试 → 桌面壳页面”。资源模型通过 oapi-codegen 的 `x-go-type` 直接指向业务类型（评审 S11），同一个 Go 类型既是 GET 响应也是同步日志快照。统一写事务放在 pgcore：账号锁、收据去重、字段级合并、变更日志、River 事务入队。

**Tech Stack:** 与工程骨架一致；新增 golang-jwt/jwt v5、x/crypto（argon2id）、aws-sdk-go-v2 S3（OSS 与 MinIO 通用，开发用 MinIO）、x/image（webp 解码）、goexif；前端 ECharts（vue-echarts）、高德 JS API 2.0 加载器。

**Spec:** `docs/api/2026-09-11-P0接口设计.md` v0.3、`docs/database/2026-09-11-P0数据库表结构设计.md` v0.3、`docs/architecture/2026-09-11-P0接口与数据设计总览.md` v0.3、`docs/requirements/2026-09-10-P0核心需求.md` v0.3。

## 全局约束

- 全部写接口：Idempotency-Key（UUID）必填；PATCH 用 If-Match 基线版本并按 changed_fields 字段级合并；DELETE、动作、PUT 要求版本相等；创建由客户端给 id。
- 可同步写事务：READ COMMITTED，第一条语句 `SELECT ... FOR UPDATE account_sync_state`；成功后写 mutation_receipts 与 sync_changes（含 changed_fields）；账号级实体的 trip_id 为空。
- 错误代码只用接口设计 1.4 清单；跨账号或跨旅行 ID 一律 404。
- 金额：Money 十进制字符串，服务端按币种小数位规范化；退款不超过原支出；净额 = 支出 − 退款。
- 会话：JWT 15 分钟（HS256，kid），refresh `<session_id>.<secret>` 30 天轮换、60 秒宽限；网页刷新凭证走 `__Host-tripfolio_refresh` Cookie + CSRF 双提交 + Origin 校验；Argon2id m=64 MiB, t=3, p=1，并发上限。
- 时间戳一律由应用时钟传入 SQL，不依赖数据库 `now()`（数据库与应用时钟可能有偏差，会触发 `updated_at >= created_at` 约束）。
- 文件：创建资产即返回上传授权；引用不要求 ready；worker 以 ETag 条件复制到最终键；下载授权原图 60 秒、缩略图 15 分钟、批量最多 100。
- 桌面壳只服务宽屏浏览器；所有页面通过 openapi-fetch 客户端访问 API，不直接 fetch。

## 每个切片的收尾验证（全部通过才能把切片标 `[x]`）

```bash
cd apps/server && gofmt -l . && go vet ./... && TRIPFOLIO_TEST_DATABASE_URL=postgres://tripfolio:tripfolio@localhost:5432/tripfolio_test?sslmode=disable go test ./... -count=1
cd apps/server && go generate ./internal/transport/httpapi/ && bash scripts/sqlc.sh generate && git status --short internal/ db/   # 生成物无差异
pnpm --filter @tripfolio/contracts build && pnpm typecheck && pnpm test && pnpm build && pnpm format:check
```

---

## 切片 1：认证与账号（AC-01）

- [x] 1.1 迁移写全 23 张表（`db/migrations/00001`–`00009`），集成测试 `TestMigrateUpIsIdempotent` 通过
- [x] 1.2 契约 `paths/auth.yaml`、`paths/account.yaml`、`paths/expense-categories.yaml` 与 Go/TS 生成物
- [x] 1.3 账号模块（identity、session、profile）与内存仓储，单元测试覆盖挑战码、密码策略、轮换宽限、重置撤销会话、限流
- [x] 1.4 PostgreSQL 适配 `adapters/postgres/account` + sqlc 查询；创建类语句的 created_at 由应用传入
- [x] 1.5 HTTP 集成测试 `tests/integration/auth_http_test.go`：注册→登录→刷新（CSRF/Origin 拒绝、宽限重试）→登出；重置密码撤销会话；PATCH /account 428/200/412；跨账号 404
- [x] 1.6 前端会话层：`stores/session.ts`（access token 内存、CSRF 镜像、account）、`api/client.ts` 中间件（Bearer 注入、401 时用 Cookie 刷新一次并重放、刷新失败清会话）、`api/auth.ts` 封装登录/注册/挑战/重置/登出；单元测试覆盖刷新重放与并发去重
  - 验证：`pnpm --filter @tripfolio/client test`、`typecheck`
- [x] 1.7 桌面壳页面：`LoginPage`、`RegisterPage`（申请验证码→倒计时→提交）、`ResetPasswordPage`；路由 `/login`、`/register`、`/reset-password`；路由守卫（未登录跳登录、已登录访问登录页跳旅行；启动时先用 Cookie 恢复会话一次）
- [x] 1.8 桌面壳账号页 `AccountPage`（`/account`）：资料编辑（昵称、时区，If-Match）、修改密码、会话列表与撤销、退出；顶栏显示昵称与入口
- [x] 1.9 切片收尾：全量验证；浏览器手工验证（`go run ./cmd/api` + `pnpm client:dev`）注册→刷新页面仍登录→退出，记录到进度日志

## 切片 2：旅行与账单分类（TR-00、TR-05 分类）

- [x] 2.1 契约 `paths/trips.yaml`、`paths/recycle-bin.yaml`、`schemas/trip.yaml`；生成
- [x] 2.2 模块 `modules/travel/trip`：创建、更新（字段级合并）、归档/恢复、删除到回收站、回收站列表/恢复/永久删除；单元测试
- [x] 2.3 PostgreSQL 适配 + sqlc；集成测试（变更日志、收据回放、版本冲突）
- [x] 2.4 分类模块补全：更新、删除（CATEGORY_IN_USE）、排序；集成测试
- [x] 2.5 桌面壳：旅行列表（按阶段分组）、新建/编辑旅行对话框、回收站页、分类管理对话框
- [x] 2.6 切片收尾验证与日志

## 切片 T：前端主题体系（插在切片 3 之前，避免后续页面返工）

详细做法见 `2026-09-13-前端主题体系.md`，设计依据 `docs/architecture/2026-09-13-前端主题体系设计.md`。

- [x] T.1 令牌契约 `shared/theme/tokens.ts`（必需令牌清单、解析、对比度）；单元测试
- [x] T.2 注册表 `shared/theme/registry.ts` 与 `themes/organic`、`themes/glass` 的令牌文件；令牌完整性与对比度测试
- [x] T.3 `styles/base.css` 与 Element Plus、Vant 两份桥接；桥接只引用契约令牌的测试
- [x] T.4 平台层 `preferences`（网页 localStorage、安卓 @capacitor/preferences）；安卓工程登记插件
- [x] T.5 `shared/theme/dom.ts` 与 Pinia `stores/theme.ts`（恢复、应用、持久化）；单元测试
- [x] T.6 启动装配：挂载前恢复主题，index.html 去掉固定 color-scheme
- [x] T.7 现有壳、页面、对话框迁移到 `--tf-*`；样式契约测试禁止字面颜色与组件库变量
- [x] T.8 主题中心页（桌面、移动）与路由 `/themes`、两壳入口；路由测试
- [x] T.9 两套主题装饰层；Chrome 与窄视口验证；better-ui / better-colors 审查
- [x] T.10 全量验证、README 与进度日志
- [x] T.11 行程玻璃主题与列表体验补修：独立内层底色、清晰状态文字；回收站／主题／刷新及行程添加改图标；分类金额仅显示有记录的分类，行李仅显示有物品的大类；全部代码修改后统一验证，不改浮层底色及业务数据
- [x] T.12 操作与行程交互优化：采用 Lucide，首页／编辑／删除／行程地图／新建待办／记账图标统一为 20px，按钮与筛选控件等高，修复提示层拦截键盘激活；行程标题与地址从 POI 自动带入，选点前置，地点记账预填备注和日期，备注／实际情况默认折叠；地图滚轮缩放并移除缩放按钮，修复算路突发请求与失败连带跳过；移除账单地图及独立退款入口；行李改为待准备／已准备圆形勾选并兼容历史三态。全部代码（含前序未完成项）修改后统一验证，详见 [交互优化设计](../../architecture/2026-09-15-行程与清单交互优化.md)

## 切片 3：行程、行李清单、待办（TR-01、TR-02、TR-03）

- [x] 3.1 契约与生成（itinerary、packing、todo）
- [x] 3.2 模块 `itinerary`（含按天重排）、`packing`（物品库与批量创建）、`todo`；单元测试
- [x] 3.3 PostgreSQL 适配、sqlc、集成测试
- [x] 3.4 桌面壳：旅行详情框架（四页签）、行程页按天编排、行李清单页、待办页
- [x] 3.5 切片收尾验证与日志

## 切片 4：账目与统计（TR-04、TR-05、TR-06）

- [x] 4.1 契约与生成（ledger、statistics）
- [x] 4.2 模块 `finance`：ledger_service（退款规则、币种锁定与解锁、字段级合并）、statistics_service；单元测试
- [x] 4.3 PostgreSQL 适配、sqlc、集成测试
- [x] 4.4 桌面壳：账单页（统计框、明细、记账与退款表单、筛选、ECharts 分类占比与每日）
- [x] 4.5 切片收尾验证与日志

## 切片 S：旅行分享（插队，优先于 5.3–5.5）

设计依据 `docs/superpowers/specs/2026-09-15-旅行分享-design.md`，详细做法见 `2026-09-15-旅行分享.md`。只依赖切片 3 与 5.1 的既有成果。

- [x] S.1 文档先行：需求、接口、数据库、总览四份设计文档升到 v0.4 并登记分享；CLAUDE.md 文档索引同步
- [x] S.2 迁移 `00010_trip_shares.sql` 与 sqlc 查询 `db/queries/travel/shares.sql`
- [x] S.3 配置 `TRIPFOLIO_WEB_BASE_URL`（生产必填 https、开发缺省 Vite 地址）与测试；`.env.example`、infra README
- [x] S.4 模块 `modules/travel/share` 的公开模型与字段白名单测试
- [x] S.5 契约 `paths/shares.yaml`、`paths/public.yaml`、`schemas/share.yaml` 与 Go/TS 生成物；处理器占位返回 503
- [x] S.6 分享服务：令牌生成、开启/重新生成/关闭、按令牌解析、访客旅行与行程列表；内存仓储与单元测试（用户已确认将设计字段表统一为每次读取旅行信息时更新时间，从 Step 3 继续）
- [x] S.7 访客算路：`geo.Service.RouteAs`、相邻路段推导、整趟缓存、失败分级；单元测试
- [x] S.8 PostgreSQL 适配 `adapters/postgres/travel/shares.go`
- [x] S.9 `middleware.Share`、`actor.Without`、路由接线与中间件/路由测试
- [x] S.10 主人侧与访客侧处理器、bootstrap 装配
- [x] S.11 集成测试 `share_http_test.go`：状态机、隔离、410/403、越权表驱动、算路降级、不写同步日志
- [x] S.12 前端主人侧：`platform.clipboard`、`shared/api/shares.ts`、`useTripShare`、分享对话框与详情页入口
- [x] S.13 前端访客通道：`ApiError` 抽到 `problem.ts`、`shareClient`、`share/api.ts`、`/s/:token` 路由、守卫跳过会话恢复、App 分流
- [x] S.14 前端访客页：`ShareLayout`、`SharePage`、`sharedRoutes`、`useSharedTrip`、路线工具泛化、样式契约扩展
- [~] S.15 切片收尾：全量验证、浏览器手工验证、进度日志

## 切片 5：文件、相册、预订与资料、地点（TR-07、TR-08）

- [x] 5.1 适配器 `objectstore`（S3 兼容：MinIO 开发、OSS 生产）、`geo`（高德代理）；compose 加 MinIO
- [x] 5.2 契约与模块 `assets`（创建即返回上传授权、下载批量授权、含 avatar 范围与账号页头像上传）、worker 校验/缩略图/EXIF
- [x] 5.G 高德完整集成（用户 2026-09-14 优先追加）：地点搜索与逆地理编码 HTTP 契约、驾车/步行/骑行算路、JS API 安全代理与地图选点、行程地点保存与相邻距离、统计地图视图（全部点位与顺序路线）；真实高德联调与回归验证。设计见 `docs/architecture/2026-09-14-高德地图与行程路线.md`，验收见 `docs/reviews/2026-09-14-高德集成验证.md`。
- [ ] 5.3 契约与模块 `album`、`reservation`；集成测试
- [ ] 5.4 桌面壳：相册页（上传进度、按天分组、编辑时刻与地点、高德选点）、预订与资料页；账单页补票据上传入口（4.3 遗留）
  - 浏览器直传前先确认桶的 CORS：**MinIO 不实现按桶 CORS**（`PutBucketCors` 返回 NotImplemented），开发环境只能改 compose 里 minio 服务的 `MINIO_API_CORS_ALLOW_ORIGIN`（已按 5173 与 Capacitor 来源收紧），`infra/minio/init.sh` 里没有也不该有 CORS 配置；生产 OSS 支持按桶 CORS，需在控制台配置允许 PUT/GET/HEAD 并暴露 ETag。前端要读 ETag 确认上传完整性，缺 `ExposeHeader: ETag` 时直传会静默拿不到 ETag。
- [ ] 5.5 切片收尾验证与日志

## 切片 6：数据管理（AC-03）

- [ ] 6.1 回收站永久清理任务、worker 阶段化清理
- [ ] 6.2 导出（ZIP、CSV）与账号注销接口；集成测试
- [ ] 6.3 桌面壳：导出页、注销流程
- [ ] 6.4 切片收尾验证与日志

---

## 进度日志

| 日期 | 任务 | 结果 | 遗留 |
| --- | --- | --- | --- |
| 2026-09-12 | 1.1–1.4 | 上个会话完成代码；本会话修复两处编译错误（`generated.Web` 枚举名、最近挑战时间查询改为 `ORDER BY ... LIMIT 1` 并处理无行）并重新生成 sqlc | 无 |
| 2026-09-12 | 1.5 | 新增 3 条 HTTP 集成测试，连续 3 轮全绿。期间发现并修复 PATCH /account 偶发 500：Docker 里 PostgreSQL 时钟比应用慢约 0.1 秒，`created_at` 走库默认 `now()` 而 `updated_at` 由应用传入，触发 `updated_at >= created_at`；现在 accounts、account_sessions、auth_challenges 的插入都由应用传时间 | 处理器日志现在附带 500 的底层原因（`cause` 字段） |
| 2026-09-12 | 1.6 | `stores/session.ts`、`api/client.ts`（401 刷新一次并重放，认证接口不重放）、`api/auth.ts`（并发刷新去重、restoreSession 只跑一次、登录/注册/挑战/重置/登出封装）；9 个单元测试通过，typecheck 通过 | 无 |
| 2026-09-12 | 1.7–1.8 | 登录、注册、找回密码、账号页与路由守卫；`useEmailChallenge` 组合式函数负责验证码倒计时；移动壳暂复用桌面页面 | 移动壳认证页在安卓阶段用 Vant 重做 |
| 2026-09-12 | 1.9 | 全量验证通过（go vet/test、生成物无漂移、typecheck、46 个前端测试、build、Prettier）。Chrome 手工验证：注册→跳旅行页→刷新页面仍登录（Cookie+CSRF 刷新）→账号页改昵称即时生效→退出后访问 /account 被守卫送到 /login?redirect=/account→登录后回到账号页 | 建议用户此时提交一次 git（切片 1） |
| 2026-09-12 | 2.1 | 契约新增 `schemas/trip.yaml`（Trip/TripListItem/TrashedTrip 通过 x-go-type 指向 `modules/travel/trip`）、`paths/trips.yaml`、`paths/recycle-bin.yaml`，共 10 个接口；`openapi.yaml` 加 travel 标签与 BadRequest 响应；TS 与 Go 生成物已更新，`travel_trip.go` 先放占位处理器保证可编译；全量验证通过 | 占位处理器在 2.3 替换为真实实现 |
| 2026-09-12 | 2.2 | `modules/travel/trip`：model/ports/service/memory_store，9 个单元测试覆盖默认值与指纹重放、校验、字段级合并（合并/相交/基线超前）、日期与时区警告、币种规则（LOCKED/AMOUNTS_EXIST/按新币种校验预算）、归档/回收站/恢复/保留期、永久清理（REAUTH_REQUIRED、任务入队、重复请求复用任务）、列表筛选排序分页与游标校验。新增公共设施：`foundation/paging`（页大小、游标信封）、`adapters/security/cursor.go`（HMAC 签名游标）、`foundation/write/memory.go`（内存 UnitOfWork，含指纹去重与 MergeSource）、`write.Scope.AddAffected`（任务等无日志资源进入 affected）、`money.Compact`/`money.FromStorage` | 修复了内存 UnitOfWork 在 Run 持锁期间回调 MergeSource 的死锁（变更历史改为独立锁） |
| 2026-09-12 | 2.3 | `db/queries/travel/trips.sql`（阶段在 SQL 中按旅行时区计算，列表用行比较走索引，ILIKE 通配符由应用转义）、`adapters/postgres/travel/trips.go`（事务内仓储 + 只读仓储，预算按币种从 NUMERIC 转回规范字符串）、`transport/httpapi/travel_trip.go` 真实处理器、`transport/river/trip_purge.go`（清理 worker 占位：记录日志并推迟 1 小时，切片 6 实现阶段化清理）、bootstrap 装配（游标签名复用 keyring）。集成测试 2 条：创建默认值与变更日志 trip_id、收据重放/指纹冲突/ID 复用、PATCH 合并与 412 冲突载荷、币种规则、跨账号 404；列表筛选/分页/游标绑定、归档、回收站、恢复（requires_snapshot）、永久清理（REAUTH、202、deletion_jobs 行与 river_job 入队、RESTORE_UNAVAILABLE、重复请求复用任务）。全量验证通过 | oapi-codegen 不校验 limit 的 minimum，处理器把显式 `limit=0` 转为无效值交服务层返回 422 |
| 2026-09-12 | 2.4 | 分类的更新（字段级合并、排序）、删除（CATEGORY_IN_USE）在切片 1 已实现，本任务补集成测试 `tests/integration/category_http_test.go`（预设种入与账号级 trip_id 为空、创建追加排序/同名 DUPLICATE/未知图标、预设改名与排序、icon 显式 null 合并与警告、412 冲突字段、空补丁 422、列表顺序、被账目引用 409（含回收站旅行内账目）、软删除后 410/列表隐藏/名称可复用/ID 不可复用）。修复一处缺陷：`CategoryService.Delete` 未传 reload，WriteResult.data 为 null，违反接口设计 1.2；现在 Create/Update/Delete 共用 `reload` 重读资源。全量验证通过 | 无 |
| 2026-09-13 | 2.5 | 完成旅行阶段分组、搜索筛选排序与游标分页、新建/编辑与字段冲突处理、归档、回收站恢复与永久清理申请、账号共用分类管理。补全类型安全 API 与组合式逻辑，修复密码复验会话刷新、原生 fetch 重放及创建已提交但响应丢失后的原样重试。需求与质量审查通过，89 项前端测试及类型检查通过；真实 Chrome 验证各流程、窄桌面布局及旅行/分类幂等重试 | 旅行详情在切片 3；实际清理 worker 在切片 6 |
| 2026-09-13 | 2.6 | 全量收尾通过：Go 格式、vet、单元与 PostgreSQL 集成测试，Go/sqlc/契约生成物无差异，前端 typecheck、89 项测试、生产构建、Prettier、差异检查。Chrome 实测登录恢复、创建校验、预算、两页冲突、归档筛选、回收站恢复与清理申请、分类管理、会话失效刷新及创建提交后丢响应的幂等恢复。详见 [切片 2 收尾验证](../../reviews/2026-09-13-切片2收尾验证.md) | 本机无 C 编译器，race 沿用 CI；建议用户检查后提交切片 2，未自动提交 |
| 2026-09-13 | T.1–T.10 | 主题体系落地：`--tf-*` 令牌契约与对比度、禁蓝紫色色相校验，Element Plus/Vant 桥接，平台层 `preferences`（网页 localStorage、安卓 @capacitor/preferences 8.0.1 已登记 Gradle），Pinia 主题 store 挂载前恢复，桌面/移动主题中心 `/themes`，现有 9 个页面与壳迁移到令牌并由样式契约测试守护；预置有机自然与玻璃态（半透明白玻璃、青绿主色）。138 项前端测试、typecheck、build、Prettier 通过；Chrome 验证两套主题下列表、新建对话框、日期弹层、主题中心与 390px 移动壳，切换后刷新保持 | 玻璃态初版蓝紫配色被用户否决并改为白玻璃，全站禁蓝紫色已写入 CLAUDE.md；ECharts 主题接入放切片 4；账号级同步未做；`.agents/`、`.claude/`、`skills-lock.json` 为本机 skill 安装产物，已加入 .prettierignore，是否入库由用户定 |
| 2026-09-13 | 3.1 | 契约新增 `schemas/itinerary.yaml`、`schemas/packing.yaml`、`schemas/todo.yaml` 与 `paths/itinerary-items.yaml`、`paths/packing-items.yaml`、`paths/todos.yaml`，共 18 个接口（含 `/packing-library`）；资源模型经 x-go-type 指向 `modules/travel/{itinerary,packing,todo}`，`PackingBatchResult` 内嵌 WriteResult。redocly lint、Go/TS 生成物、前端 typecheck 与 Prettier 通过 | 处理器已接入生成接口，但三个服务在 bootstrap 尚未装配（Deps 为 nil 时接口返回 503 DEPENDENCY_UNAVAILABLE），3.3 接入 PostgreSQL 后补齐 |
| 2026-09-13 | 3.2 | `modules/travel/itinerary`：创建追加到当天末尾、字段级合并（scheduled_on/sort_order 不在 Patch 字段集，编辑与重排不互相冲突）、坐标成对与 6 位精度、结束/时长互斥、金额与旅行币种一致（CURRENCY_MISMATCH）、越界日期警告、重排一事务（集合不符 ORDER_CHANGED、版本不符 412、只写实际变化行）。`modules/travel/packing`：同分类同名唯一（改名同样校验）、批量创建跳过既有与请求内重复并从 affected 推导 created_ids/skipped、内置物品库 7 类 55 项随程序嵌入。`modules/travel/todo`：completed_at 由服务端维护且重复置完成不移动时间、due_on 空值最后哨兵游标、逾期按旅行时区当天计算（含跨日测试）。三包合计 33 个单元测试通过，全部经 MemoryUnitOfWork 覆盖幂等重放与指纹冲突 | 无 |
| 2026-09-13 | 3.3 | `db/queries/travel/{itinerary,packing,todo}.sql`（共用 `GetTripContentInfo` 读旅行币种/时区/日期/回收站状态；行程列表与重排锁定走 `(scheduled_on, sort_order, id)` 行比较；待办用 `COALESCE(due_on, 9999-12-31)` 哨兵与索引表达式一致；物品同名预检 `lower(btrim(name))`），`adapters/postgres/travel/{itinerary,packing,todo}.go`（currency_code 按旅行币种派生、坐标 NUMERIC↔float64 六位小数、当地时间 timestamp(0)↔LocalDateTime）。bootstrap 装配三个服务并共用游标签名器，集成测试 fixture 同步。新增 `tests/integration/trip_content_http_test.go` 3 条：行程创建默认值/越界警告/币种不符/互斥校验/PATCH 清空与合并冲突/跨日重排与 ORDER_CHANGED、412/游标分页/删除 410/旅行回收站后 410/跨账号 404；��李物品库/同名冲突/批量跳过与重放/状态筛选/改名撞车/删除后复用；待办完成时间/排序与哨兵游标/四种筛选/逾期标识/完成与取消/清空截止/合并冲突/删除。全量收尾验证通过（Go 全部测试、生成物无漂移、typecheck、138 项前端测试、build、Prettier） | 3.1 遗留的“服务为 nil 返回 503”已解除；无 |
| 2026-09-13 | 3.4 | 详情页 `/trips/:tripId` 五个子路由页签（行程/账单/行李清单/相册/待办，账单与相册为占位卡片，待办按用户决定暂作第五页签），`tripContext` 向页签提供旅行、旅行时区今天与币种精度，`DesktopShell` 的 RouterView 按 tripId 作 key。共用层：`useItemEditor` 泛型编辑流程（从 useTripEditor 抽出：基线/412 冲突对比与对最新版本重提/幂等意图/创建不确定原样重放）、三个 draft 校验模块、`tripDays`（"9.20 周日" 标题、旅行日期外项目归入额外的天、按旅行时区取今天）、`useItineraryBoard`（一次拉全、按天分组、拖动落到 reorder 接口并提交源日+目标日完整集合与版本）。行程页用 vue-draggable-plus 0.6.1 拖拽（同日与跨日）；编辑对话框含结束时间/停留时长互斥切换、预计费用按旅行币种、实际记录。行李页按七分类分组、进度按条目、三态推进与回退、物品库勾选批量加入并标记已在清单、自定义物品。待办页四态筛选、逾期用 `--tf-danger` 标记、勾选直接 PATCH。新增 34 个单元测试（tripDays、useItemEditor、三个 draft、useItineraryBoard、路由），前端 172 项全绿；typecheck、build、Prettier、样式契约通过。Camoufox 实测（API 重启后）：创建旅行→详情→按天添加行程→编辑金额与时长→物品库加入 3 件与重复跳过→状态推进→待办逾期/筛选/勾选完成→404 旅行错误页；Playwright 真实鼠标拖拽跨日与同日重排，抓到的 reorder 正文与设计一致 | 手工验证发现并修复：VueDraggable 挂载时 v-model 若非数组不会初始化 Sortable，镜像改为 days 的 immediate watcher 先铺满；窄视口页签标签补 nowrap。经纬度与高德选点留到切片 5；`AccountPage.vue` 有一处 18:53 的本地改动（会话列表改为骨架屏与列表），不属于本任务，未处理 |
| 2026-09-13 | 3.5 | 全量收尾通过：Go 格式、vet、17 个包的单元与 PostgreSQL 集成测试，Go/sqlc/契约生成物重新生成后无差异，契约 lint，前端 typecheck、172 项测试、生产构建、Prettier、diff check。真实接口验证（Camoufox 交互 + Playwright 真实鼠标拖拽）覆盖详情页、行程按天与拖拽重排、物品库批量加入与状态推进、待办筛选与逾期、错误态与 390px 窄视口。详见 [切片 3 收尾验证](../../reviews/2026-09-13-切片3收尾验证.md) | 本机无 C 编译器，race 沿用 CI；建议用户检查后提交切片 3，未自动提交；`AccountPage.vue` 本地改动与两个未跟踪的玻璃主题文件请一并决定 |
| 2026-09-13 | 4.1 | 契约新增 `schemas/ledger.yaml`（LedgerKind/LedgerEntry/LedgerCreate/LedgerPatch/LedgerPage）、`schemas/statistics.yaml`（TripStatistics 及 scope、totals、trip_budget、by_category、daily 子模型）、`paths/ledger-entries.yaml`、`paths/statistics.yaml`，共 6 个接口；`common.yaml` 增加 `SignedMoney`（可负的净额与剩余预算）。LedgerEntry 与 TripStatistics 经 x-go-type 指向 `modules/finance`。redocly lint、Go/TS 生成物、Prettier 通过；处理器 `finance_ledger.go` 已接入生成接口，Deps 新增 Ledger/Statistics，为 nil 时返回 503 | 4.3 接 PostgreSQL 后装配 |
| 2026-09-13 | 4.2 | `modules/finance` 新增 `LedgerService`：金额按旅行币种规范化（请求币种不符 CURRENCY_MISMATCH，JPY 小数被拒）、occurred_on 默认旅行时区今天、分类与票据引用校验（INVALID_REFERENCE）、退款关联规则（原支出须为同旅行有效支出、分类一致、关联合计不超额 REFUND_AMOUNT_EXCEEDED、原支出减额同样受限）、修改原支出分类同事务更新关联退款并进入 affected、删除原支出解除关联退款（changed_fields=['refunded_entry_id']，warnings REFUNDS_UNLINKED）、第一条账目锁定旅行币种/最后一条删除后解锁（旅行进入 affected，changed_fields=['currency_locked_at']）、字段级合并与 412 冲突、列表五种筛选与降序游标。`StatisticsService`：净额、预算余额/超支（基于整趟）、分类汇总含被引用的已删除分类、占比仅总净额>0 且各分类净额>=0 时提供、每日明细降序分页。新增 `foundation/money.Decimal`（big.Int 定点 4 位）供汇总与净额计算。13 个 finance 单元测试 + 2 个 decimal 测试通过；全量 Go 测试、生成物无漂移、typecheck、172 项前端测试、build、Prettier 通过 | 退款汇总按内存/SQL 双实现，SQL 版在 4.3 落地；票据资产校验依赖 assets 表，4.3 用 `assets` 表现有列实现 |
| 2026-09-14 | 4.3 | `db/queries/finance/ledger.sql`（账目 CRUD 与 `sqlc.embed` 连表派生 currency_code；票据引用用 `unnest WITH ORDINALITY` 整体替换并按 sort_order 还原；关联退款 `FOR UPDATE` 锁定；列表用 `(occurred_on, id) <` 行比较走降序部分索引；统计四条聚合 CASE WHEN 净额，分类聚合 LEFT JOIN + HAVING 保留被引用的已删除分类）、`adapters/postgres/finance/ledger.go`（事务内仓储 + 只读仓储；统计在 REPEATABLE READ 只读事务中读取旅行、筛选合计、整趟合计、分类与每日；票据资产校验查 `assets` 同旅行未删除图片行；币种锁定复用导出的 `travelpg.ToTripResource`）。bootstrap 装配 Ledger/Statistics。新增 `tests/integration/ledger_http_test.go` 3 条：创建默认值/币种锁定与旅行 affected 及变更日志/CURRENCY_LOCKED 联动/校验与引用/票据顺序与清空/合并与冲突/幂等重放与 ID 复用/跨账号 404；退款分类一致与超额/原支出减额受限/改分类联动/关联筛选/解除关联/删除原支出解除退款与 changed_fields/最后一条删除解锁并可改币种/回收站 410；列表排序筛选与游标绑定/统计总额预算分类占比每日/日期筛选禁用占比与每日分页/分类筛选/CATEGORY_IN_USE 与删分类后统计。全量收尾通过（Go 全部测试、Go/sqlc/契约生成物无漂移、typecheck、172 项前端测试、build、Prettier、diff check） | 生成层不校验枚举与未知字段：非法 kind 和 PATCH 里的 kind 分别由服务返回 422 VALIDATION_FAILED（与 state/limit 一致）；票据资产由测试直接插表，资产接口在切片 5 |
| 2026-09-14 | 4.4 | 账单页 `LedgerTab`：统计框（净支出、支出/退款、账目数、总预算就地编辑、剩余预算与超支）、日期与分类筛选同时作用于统计与明细（类型只筛明细）、ECharts 分类占比环图与每日净支出柱图、分类金额表（图表的表格视图，点击进入该分类明细）、账目明细列表与游标翻页。新增 `api/ledger.ts`、`api/statistics.ts`、`travel/ledgerDraft.ts`（金额须为正、日期必填以免 PATCH 发出契约不允许的 null、退款关联原支出时自动带出并锁定分类、kind 不进 PATCH）、`travel/statisticsView.ts`（千分位、定点求和、饼图切片折叠）、`theme/chartTokens.ts`（运行时读 `--tf-*` 并随主题切换刷新，ECharts 不能写 var()）。**按 dataviz 技能校验发现原 `--tf-chart-1..6` 不合格（相邻色 CVD ΔE 低至 2.4、正常视力低于阈值），重做两套主题图表色板**并加回归测试（相邻色 OKLab ΔE ≥ 15）。真实 Chrome 验证：记支出/退款、退款关联原支出后分类自动锁定、净额与分类冲减、总预算就地编辑与超支、主题切换后图表重新取色（画布像素确认为新令牌）、点击分类与点击柱子筛选、删除原支出解除关联、仅剩退款时净额为负且不画无意义占比图。前端 typecheck、195 项测试、build、Prettier 全绿 | 修复：`REFUNDS_UNLINKED` 未登记中文文案，删除原支出时把原始代码抛给了用户；已补文案并加测试断言服务端 4 个警告代码都有文案。票据附件选择依赖 assets（切片 5），本次表单未含。LedgerTab 懒加载块含 ECharts 约 194 kB gzip，仅访问账单页时加载。Go 侧验证与生成物漂移检查留到 4.5 |
| 2026-09-14 | 4.5 | 切片 4 收尾全量验证通过：`gofmt -l .` 无输出、`go vet ./...` 干净、`go test ./... -count=1`（带 TRIPFOLIO_TEST_DATABASE_URL）全部包通过（含 `modules/finance`、`tests/integration` 6.2s）；生成物漂移改用哈希法核验（重新跑 `go generate` + `sqlc.sh generate` 后 14 个生成文件哈希完全一致，契约 `dist/openapi.v1.yaml` 与 TS 类型二次打包后亦一致）——本切片整体未提交，`git status` 必然列出全部新增文件，不能当作漂移判据；前端 contracts build、typecheck、195 项测试、build、Prettier 全绿 | 验证时另一会话正在同工作区做 5.1，其 `go.mod`/`go.sum`（aws-sdk-go-v2，尚未 tidy，暂挂 indirect）与 `internal/adapters/objectstore/` 属于切片 5，已排除在切片 4 结论之外；对方确认该目录自测绿且验证期间冻结 Go 改动。本机无 C 编译器，`-race` 沿用 CI。建议用户检查后提交切片 4，未自动提交 |
| 2026-09-14 | 5.1 | `adapters/objectstore`：`Store` 接口与 `S3Store`（aws-sdk-go-v2，MinIO 路径寻址 / OSS 虚拟主机寻址只差 `Config`）；`KeysFor` 按 account_id、asset_id、upload_attempt 推导 `staging/`、`assets/`、`thumbnails/` 三个互不包含的前缀；直传授权把 Content-Type 与 Content-Length 写入签名，下载授权固定 response-content-type 与 content-disposition 并按 RFC 5987 编码文件名；`CopyIfMatch` 以校验时 ETag 为条件复制到最终键；S3 错误折叠为 `ErrNotFound`/`ErrPreconditionFailed`，删除不存在的键视为成功。`adapters/geo`：高德逆地理编码与输入提示代理，按账号限流（复用 `ratelimit`）、逆地理编码按坐标 3 位小数缓存 24 小时（无结果也缓存）、全局日上限跨天重置、`flexString` 兼容高德空字段返回 `[]` 而非 `""`、无坐标条目（公交线路）剔除、空结果映射 `ErrNoResult`。config 增加 `ObjectStore`/`Geo` 两组配置（生产必填校验、`USE_PATH_STYLE` 默认跟随环境），bootstrap 装配且未配置时为 nil 并告警降级。compose 加 MinIO（9000/9001）与一次性 `minio-init`（建私有桶 + `staging/` 1 天过期）。测试：objectstore 10 个单元 + 3 个集成（真实 MinIO 跑通直传→Head→错误 ETag 被拒→条件复制→中文文件名下载→不存在对象→幂等删除，未设端点时跳过）、geo 14 个单元（httptest 桩）、config 4 个新增。全量 Go 验证通过（gofmt、vet、20 个包 test），生成物哈希无漂移，契约与 Prettier 通过 | 踩到三个真实坑并修正：mc 镜像无 `awk`（改纯 sh 拼接）、`mc cors set` 要 XML 不是 JSON、**MinIO 根本不实现按桶 CORS**（PutBucketCors 返回 NotImplemented），最终改为服务端全局 `MINIO_API_CORS_ALLOW_ORIGIN`，已用真实预检请求验证白名单来源放行、`evil.example` 不放行；生产 OSS 支持按桶 CORS，需在控制台配置。初版脚本把 awk 缺失误报成「mc 不支持 cors」，已改为失败即暴露真实原因并退出。`/geo` HTTP 接口与 `x/image`、goexif 依赖属 5.2；本机无 C 编译器，race 沿用 CI |
| 2026-09-14 | 5.2 | 契约 `schemas/asset.yaml`（Asset 经 x-go-type 指向 `modules/assets`、AssetWriteResult 内嵌 upload_authorization、DownloadAuthorization 批量）与 `paths/assets.yaml` 共 7 个接口，`openapi.yaml` 新增 assets 标签与 `DependencyUnavailable` 响应；大小与类型拒绝用已登记的 413 REQUEST_TOO_LARGE / 415 UNSUPPORTED_MEDIA_TYPE，不新造错误码。`modules/assets`：`Service` 实现上传状态机（uploading→confirm→processing→worker→ready/failed；未过期 uploading 续签同暂存键、failed 与过期 uploading 开新尝试换键、processing/ready 一律 409 UPLOAD_NOT_ALLOWED、超窗确认 410 UPLOAD_EXPIRED）、直传签名用创建时声明的 expected_size/declared_media_type、下载授权仅 ready（原图 60 秒/缩略图 15 分钟）、批量含他人资产整批 404、`AvatarUsable` 实现切片 1 预留的 `account.AvatarChecker`；`Verifier` 在事务外读暂存对象并校验大小/嗅探类型/摘要，以校验时 ETag 条件复制到最终键，事务内复核状态后写 ready 或 failed（8 个可公开失败码写入 error_code），图片另起事务生成缩略图且失败不影响原图；worker 写事务用 `(asset, attempt)` 派生确定性 OperationID，River 重试命中收据。`adapters/imaging`（DecodeConfig 先拒解压炸弹、EXIF 拍摄时间与 WGS-84 坐标、512 长边 JPEG 缩略图不放大、魔数嗅探）与 `adapters/objectstore.AssetsStore` 两个到模块接口的薄适配；`db/queries/assets/assets.sql` + `adapters/postgres/assets`；`transport/httpapi/assets.go`；`transport/river/asset_verify.go`（重试耗尽把资产标 PROCESSING_FAILED 而不是永远卡 processing）；bootstrap 装配 Service/Verifier 并把资料服务接上头像校验，worker 复用 BuildServices。前端 `api/assets.ts`（XHR 直传报进度、跳过浏览器禁设的 Content-Length、SubtleCrypto 摘要）、`useAssetUpload`（登记→直传→确认→轮询到 ready/failed，重试复用资产开新尝试）、账号页头像上传/更换/移除/重试与短期签名预览。测试：assets 模块 37 个单元（含 12 个校验器）、imaging 10、objectstore 适配复用既有、前端 useAssetUpload 8；`tests/integration/asset_http_test.go` 6 条在真实 MinIO + PostgreSQL 上跑通完整链路、损坏重传、嗅探为准与 PDF 无缩略图、创建拒绝、列表/批量/跨账号/幂等重放、头像绑定四态。Go 全部包 + 前端 203 项 + typecheck + build + Prettier 全绿，生成物哈希无漂移。真实 API + 真实 worker 手工跑通：直传 → 确认 → 校验 ready（类型/字节/1400×900/摘要一致）→ 原图下载（no-store、中文名 RFC 5987、字节一致）→ 缩略图 512×329 → 头像绑定；伪造的只有魔数的 JPEG 被正确判 CORRUPT_IMAGE | **设计矛盾并已按「先改文档」修正**：`sync_changes_account_scope` CHECK 只允许 expense_category 的 trip_id 为空，头像资产创建曾 500；数据库设计第 14 节「avatar 不进入旅行流」已明确为「不写 sync_changes，走收据；其他设备经 GET /account + 下载授权取头像」，代码用 `recordWrite` 统一处理。三次变异注入（确认直接置 ready、过期仍续签、ETag 不匹配不判失败）与前端一次（确认后不轮询）均被测试抓住。集成测试改 `apiFixture` 按 `TRIPFOLIO_TEST_OBJECTSTORE_ENDPOINT` 条件接 MinIO 并暴露 `services`，未设端点时资产用例跳过。CLAUDE.md 说的「modules 不导入 adapters」被 `account→mail` 先例违反，本切片未沿用（imaging/objectstore 反向适配到模块接口）。收尾时误杀了 8080 上另一会话的 API（`go run` 产物同名 api.exe），已带对象存储配置重启，登录会话因临时密钥失效需重登。移动壳头像与 EXIF 坐标 WGS-84→GCJ-02 客户端转换留 5.4 |
| 2026-09-14 | 5.G | 高德从适配器补齐到真实页面闭环：新增认证的 /geo/reverse-geocode、/geo/places、/geo/routes 契约与处理器，modules/geo 隔离供应商，驾车／步行／骑行返回真实道路米数、秒数与 GCJ-02 折线。前端搜索／地图／坐标选点保存到每日行程，自动计算相邻段，行程与账单统计入口展示整趟或单日地图；无坐标、跨日、失败段与示意接续明确标注。修正逆地理缓存导致坐标漂移、真实骑行 duration 字段、绕行路线适配、默认整趟选项、选点解析期间误保存；移动壳补复用组件样式与 tripId 隔离。用户两套凭证只存忽略的本地配置，Vite／Caddy 服务端追加安全密钥，Compose 透传 Web 服务配置。真实高德五项冒烟和 Chrome 完整流程通过；Go 全量与专项、235 项前端测试、typecheck／build／格式检查通过，15 个 Go/sqlc 与 2 个契约生成物哈希稳定。详见 [高德集成验证](../../reviews/2026-09-14-高德集成验证.md) | 未提交，原 5.2 资产改动保留；旧开发 API 需重启加载路由。生产公网、安卓真机与相册／预订选点尚未验收；单进程限额重启会重置，不能代替高德月配额管理。 |
| 2026-09-15 | T.11 | 行程玻璃内层改用独立半透明令牌，状态文字保持清晰；回收站／主题／刷新及行程、每日、行李分组添加统一图标，保留提示、无障碍名称、键盘操作和加载禁用；分类金额仅显示当前范围有收支的分类（含全额退款净额零），行李仅显示筛选后有物品的大类且保留全部添加选项。全部代码完成后统一验证：35 个文件、246 项前端测试、Vue／TypeScript 检查、生产构建、相关格式与差异检查通过；Chrome 覆盖两套主题、桌面与 390px 移动壳，并检查行李页 320px 无溢出。详见 [界面图标与分组验证](../../reviews/2026-09-15-界面图标与分组验证.md) | 未提交，既有高德及资产改动保留；浏览器使用隔离示例数据，未修改用户业务数据；安卓真机、完整屏幕阅读器与生产环境未验证。既有账单／ECharts 大包提示保留。 |
| 2026-09-15 | T.12 | 操作与行程交互优化：Lucide 图标统一 20px 并抽出 ActionIcon／IconAction，控件高度收敛到 `--tf-control-size`（窄屏 44px），提示浮层不再拦截键盘激活；行程标题与地址从 POI 自动带入、选点前置到表单顶部、地点记账预填备注与日期、备注与实际情况默认折叠；地图改滚轮缩放并移除缩放按钮；算路用 AbortController 加代际校验收敛突发请求，单段解析失败不再连带跳过后续路段；账单页移除独立退款入口（会话中曾加入的账单地图一并移除，最终形态是独立的行程地图页签）；行李改为待准备／已准备两态勾选并兼容历史三态。与切片 5.1／5.2／5.G 的既有改动一并验证通过：gofmt 无输出、go vet 干净、Go 全部包测试 0 失败（含真实 MinIO 与 PostgreSQL 集成测试）、15 个 Go/sqlc 生成物与 2 个契约产物哈希无漂移、typecheck、38 个文件 291 项前端测试、生产构建 exit 0、Prettier 全绿。已提交 `8a4209b`（128 个文件，+18616/−803） | 未推送，由用户决定；5.3／5.4 相册与预订、5.5 收尾仍未开始，账单页票据上传入口仍挂在 5.4；安卓真机、完整屏幕阅读器与生产环境未验证；LedgerTab 的 ECharts 分块体积提示为既有情况，未处理 |
| 2026-09-15 | S.1–S.5 | 旅行分享的文档、迁移、配置、公开模型与契约就绪。S.1 需求／接口／数据库／总览四份文档升 v0.4（TR-09、SHARE_NOT_FOUND、2.11／3.11、第 24 张表 trip_shares），CLAUDE.md 索引同步为 96 个接口／24 张表；S.2 迁移 `00010_trip_shares.sql`（明文令牌唯一索引、复合外键级联、令牌格式 CHECK）与 `db/queries/travel/shares.sql` 六条查询，sqlc 生成 `shares.sql.go` 且 `TestMigrateUpIsIdempotent` 通过；S.3 `config.WebBaseURL`（开发缺省 `http://localhost:5173`、去尾斜杠、生产强制 https）与 `.env.example`、infra README；S.4 `modules/travel/share/model.go` 的 `PublicTrip`(5 字段)／`PublicItineraryItem`(12 字段) 与 3 个白名单／防泄露测试；S.5 契约 `schemas/share.yaml`、`paths/shares.yaml`、`paths/public.yaml` 共 7 个接口与 `shareAuth` 安全方案、public 标签，Go／TS 生成物更新，`share.go`／`public.go` 先放 `notWired()` 占位。验证：gofmt 无输出、go vet 干净、Go 单元测试全绿、`go generate` 二次执行 api.gen.go 哈希不变、redocly lint 无 error、pnpm typecheck 通过 | 生成物与计划预测有两处不同（已适配、未改生成代码）：`GetSharedTrip200JSONResponse` 是 `{Body PublicTripResponse; Headers}` 而非 `{Data}`，`GetTripShare/Enable/Rotate200JSONResponse` 是 `TripShareResponse` 的类型别名；`config_test.go` 既有 `TestProdDefaultsToVirtualHostAddressing` 需补 `TRIPFOLIO_WEB_BASE_URL` 才能过生产 https 校验。S.6 的 `ports.go`／`context.go`／`memory_store.go` 已按计划写好（Step 1–2），S.6 Step 3 起未开始；S.7–S.15 未开始，交由后续会话继续 |
| 2026-09-15 | S.6 接手核对（受阻） | 已按顺序读取 CLAUDE.md、分享设计与实施计划，并核对 S.1–S.5 状态及 S.6 Step 1–2 交接。发现设计 §3 字段表（第 78 行）仍要求 last_viewed_at 按小时节流；其规则正文已按 S.1 Step 4 改为每次读取旅行信息更新，但 S.6 ViewTrip 与现有 RecordTripShareView SQL 每次都更新时间，字段表与规则正文／计划不一致。按用户“发现计划与设计冲突就停止”的要求，在写 Step 3 测试前暂停，S.6 标为 [!] | 待确认是否将字段表统一为每次访客读取旅行信息更新；未改业务代码、设计或 SQL，未运行本轮红绿测试及后续验证；S.7–S.15 未开始，未提交或推送 |
| 2026-09-15 | S.6 | 用户确认后统一 last_viewed_at 字段说明；新增服务测试，首跑因 Service/NewService/Deps/GenerateToken 未定义失败，按计划实现后 share 包全部通过；gofmt -l . 无输出、go vet ./... 通过。实现令牌、主人状态机、访客账号隔离读取、分页和访问计数。 | S.7 缓存类型按计划提前放入 routes.go 供 S.6 编译；无提交。 |
| 2026-09-15 | S.7 | RouteAs 与 Routes 测试首跑因方法未定义失败；实现后 geo/share/transport 全部通过，gofmt 无输出、go vet ./... 通过；覆盖跳过无坐标点、跨日接续、模式隔离、坐标变化及过期重算、单段失败继续、配额耗尽和分享/IP 限流。 | 无偏离；限流和缓存为单进程状态。 |
| 2026-09-15 | S.8 | 补真实数据库仓储测试以满足逐任务红绿要求：首跑 NewShareStore 未定义失败，实现后 go test ./... -count=1（显式测试库 URL）全包通过，集成测试 11.129s；gofmt 无输出、go vet ./...、go build ./... 通过；验证跨账号隔离、重复开启、轮换、关闭、计数、版本及日志不变。 | 生成类型与交接一致，无需改 SQL/生成代码；额外新增 share_store_test.go。 |
| 2026-09-15 | S.9 | 中间件首跑因 Share 未定义失败；路由测试先因缺 X-Robots-Tag 失败；补齐 actor.Without、独立 Viewer 上下文、中间件和路由后 transport 全包通过，gofmt 无输出、go vet ./... 通过。 | 计划路由测试缺分享头却期望未装配服务 503，已给该用例加 Share 头使其测试预期场景，保留 503 断言及中间件缺头 401 测试。 |
| 2026-09-15 | S.10 | 七个处理器身份隔离测试首跑均返回占位 503 而非应有 401，替换处理器及 bootstrap 装配后通过；go build ./...、gofmt、go vet ./... 和带测试库 URL 的 go test ./... -count=1 全部通过（集成 7.181s）。 | 按已知生成物将 GetSharedTrip 成功响应适配为 Body/PublicTripResponse，未改生成代码；新增 share_handlers_test.go 满足红绿要求。 |
| 2026-09-15 | S.11 | 新增分享 HTTP 集成测试，首跑因预测的双层 data 与夹具缺 WebBaseURL 失败；按真实契约修复夹具/取值后 5 个分享集成测试通过。保留原越权表并自动遍历设计+契约共 88 个账号接口一律 401；补同账号跨旅行/游标隔离及全部 public 路径缺头/Bearer 拒绝。gofmt、vet、带测试库 URL 的 Go 全量通过（集成 8.808s），go generate/sqlc 重生成 16 个文件 MD5 无漂移。 | 计划测试将单资源误当 WriteResult 双层 data，已适配未放宽断言；88 含当前契约独有的分类详情和密码修改两个接口；无提交。 |
| 2026-09-15 | S.12 | useTripShare 首跑模块不存在失败；补齐平台剪贴板、主人 API、组合式状态机、确认对话框及详情页图标入口后 4 个专项测试通过；全客户端 39 文件 298 项测试、pnpm typecheck、格式化通过。 | 沿用既有 --tf-success 主题令牌；未添加依赖。 |
| 2026-09-15 | S.13 | 客户端/路径模块、守卫、路由注册测试先失败，落地后通过；新增实际导航及壳切换测试抓到会话 store 被提前读取、静态 App 根组件不随路由切换，修复为分享守卫先分流并延迟导入认证模块、App 按 route.meta 选择懒加载布局后通过。全客户端 44 文件 311 项测试、typecheck 通过。 | 保留计划 share 启动 prop 作为路由就绪前的回退；401/404 分别验证不重放；新增 2 个边界测试文件，未改设计。 |
| 2026-09-15 | S.14–S.15 | 访客页落地：`ShareLayout`、`SharePage`（按天行程与地图两视图）、`SharedItinerary`、`SharedMap`、`sharedRoutes` 路线工具泛化、`useSharedTrip` 错误分级、样式契约扩展到访客页。切片自动化收尾全通过：`gofmt -l .` 无输出、`go vet ./...` 干净、带测试库 URL 的 `go test ./... -count=1`（真实 PostgreSQL 与 MinIO，含 `modules/travel/share`，集成 27.1s）0 失败；重跑 `go generate` 与 `sqlc.sh generate` 后 16 个 Go 生成物 MD5 无漂移，契约 lint `valid` 且 `dist/openapi.v1.yaml` 与 `generated/openapi.v1.d.ts` 重新打包后 MD5 无漂移；`pnpm typecheck`、48 文件 330 项前端测试、`pnpm build`（exit 0）、`pnpm format:check` 全绿；产物中 `SharePage-*.js` 与 `DesktopShell-*.js`／`MobileShell-*.js` 无交集，访客页不加载桌面或移动壳 | 浏览器手工验证发现两处与既有页面的不一致，S.15 保持 `[~]` 待修复后收尾：分享行程页相邻地点之间未显示距离；分享地图未按既有页面展示沿途各站与「驾车 131.2 公里 · 2 小时 3 分钟／跨日接续」整段摘要。环境备注：本机 PATH 上的 DSH pnpm 包装器把 `node` 解析成 DSH Desktop.exe，node 脚本类 bin 的 argv 被移位，`pnpm build`／`pnpm lint` 里 redocly 报未知参数而失败，把系统 pnpm 目录（`%APPDATA%\npm`）前置到 PATH 后完整通过；`LedgerTab` 的 ECharts 分块体积提示为既有情况 |
| 2026-09-15 | S.15 修正 | 手工验证发现的两处不一致已修：访客行程视图复用主人侧「相邻地点路程」（下一站、出行方式、距离与用时、跨日接续、中间未定位项目，并加同一组出行方式切换），地图视图复用「沿途各站」列表与汇总（已定位站数、道路总里程、预计用时、段数进度与图例）；算路时机从「切到地图视图」改为「拿到行程即算」，两个视图共用 `buildSharedMap` 新增的按出发站索引结果。按「文档先行」先改设计 6.1／7.1 与详细计划的 S.14 修订说明，再改代码。验证：第 2、3 次重跑后 48 文件 334 项前端测试、typecheck、build、Prettier 全绿；用真实分享链接（旅行「大兴安岭」，6 个已定位站点）在 Chromium 实测行程视图与地图视图、1440px 与 390px 两种宽度，站间距离与沿途各站均按主人侧文案渲染（如「驾车 131.2 公里 · 2 小时 4 分钟」「跨日接续」），窄屏正确堆叠无溢出；无痕上下文全程无 `/auth/*` 请求，`/public/trip`、`/public/itinerary-items`、`/public/routes` 三个响应都带 `X-Robots-Tag: noindex, nofollow` | 未提交，由用户决定；分享的「重新生成／关闭／旅行移入回收站后失效」流程与安卓真机仍未人工走查（这三个操作会改动用户当前的分享状态，未自动执行），S.15 保持 `[~]`；`.tools/` 下的校验脚本与截图为本机产物，按 `.gitignore` 不入库 |
| 2026-09-15 | T.12 追加 | 地图滚轮缩放改为以**指针位置**为锚点：原先每次滚轮都把中心设回原中心，指针下的地点会整体漂走。现在先按缩放倍数算出新中心在缩放前容器像素坐标中的位置（容器中心 O、指针 P、倍数 S＝2^Δzoom 时中心沿 O→P 移动 (P−O)×(1−1/S)），再用 `containerToLngLat` 换成经纬度后设置；容器无布局或 SDK 无像素转换时退回原来的中心缩放，到达 2–20 级边界时不再重设视角但仍阻止页面滚动。换算抽成纯函数 `cursorZoomCenter` 单测精确值，组件侧断言交给 SDK 的锚点像素与是否走兜底路径。按「文档先行」先改交互优化设计与高德集成设计两处滚轮规则再改代码。验证：`amap.spec.ts` 11 项、全量前端测试、typecheck、build、Prettier 全绿；真实 Chromium 在分享地图上量测（指针停在 4 号标记上滚轮放大一级）整体跨度 529.5px→1057.5px，**锚点标记位移 0.9px**，其余标记位移 71–529px | 未提交，由用户决定；地图仍以刻度级别缩放，未做手势双指缩放与惯性；分享的重新生成／关闭／回收站失效流程与安卓真机仍待人工走查，S.15 保持 `[~]` |
| 2026-09-15 | S.15 追加 | 访客地图补齐主人侧的日期范围筛选（整趟旅行／单日）：筛选只作用于地图，单日只显示该天点位、连到次日的路段随之消失，汇总、沿途各站与空状态都按筛选后的范围计算；整趟路段由服务端一次返回，按天筛选在本地 `buildSharedMap` 完成，实测全程只发 1 次 `/public/routes`。按「文档先行」先补分享设计 6.1 与详细计划的 S.14 修订说明，再改代码。验证：`SharePage.spec` 新增筛选用例（11 项）后全量前端测试、typecheck、build、Prettier 全绿；真实 Chromium 实测筛选前 6/6 站、1049.1 公里、5/5 段且含跨日接续，选「9.21 周一」后 3/3 站、129.1 公里、2/2 段且无跨日接续，切回整趟恢复原值，算路请求始终为 1 次 | 未提交，由用户决定；访客地图只按数量提示未定位项目，不含主人侧的「待补充位置」跳转按钮（访客不能编辑）；主人侧下拉的日期来自行程项目，旅行日期外的空白天不出现，与主人侧一致；S.15 保持 `[~]` |
| 2026-09-15 | 部署改造 | 后端合并为单二进制：删除 `cmd/api`、`cmd/worker`、`cmd/migrate`，新增 `cmd/tripfolio`（`serve` 等待数据库 → 自动迁移 → 同进程启动 HTTP 与 River worker；另有 `migrate`／`healthcheck`／`version`）。前端产物经 `go:embed all:dist` 嵌入 `internal/web/`，静态资源、SPA 回退与高德 `/_AMapService` 代理（服务端覆盖 jscode、剥掉站点 Cookie 与 Authorization）都在进程内完成，**不再需要 Caddy**：`infra/caddy/` 已删除，`infra/compose.yaml` 的 edge profile 与 worker 服务移除，`app` profile 改为单二进制容器。启动失败可诊断：阶段化日志（phase 字段）、stderr 中文排查块（不受日志级别影响）、退出码 2 配置／3 数据库／4 迁移／5 装配与任务／6 HTTP 监听。部署面 `docker/` 重写为「单个 app 服务 + 已有 PostgreSQL + 阿里云 OSS」，容器启动自动迁移。按「文档先行」先写 [单二进制部署设计](../../architecture/2026-09-15-单二进制部署设计.md) 并修正技术选型与高德文档里的 Caddy 入口描述。验证：Go 全量测试（含真实 PG 与 MinIO 集成）、`gofmt`／`vet` 干净、前端 typecheck 与 348 项测试、`pnpm build`、Prettier 全绿；本机打包出 20 MB 二进制后实跑（内嵌页面、`/api/unknown` 保持 problem+json、真实高德上游代理 200）；容器对着宿主机外部 PostgreSQL 启动并自动迁移 10 个版本（30 张表、healthy） | 未提交，由用户决定；Caddy 移除后 HTTPS 需要外层代理（纯 http 部署见 docker/README）；内置静态资源意味着改前端必须重新打包 |
| 2026-09-15 | 部署评审修复 | 按评审清单逐条修复并复核：①接口前缀空串 bug——`VITE_API_BASE_URL` 在 Docker 里被传成空串而客户端用 `??` 兜底，容器内 baseUrl 变空、请求被 SPA 回退吞掉；抽出 `resolveApiBaseUrl` 用 `||` 兜底并加回归测试（临时改回 `??` 时 3 个用例失败可复现），Dockerfile 去掉空默认值、compose 不再传空串；②补 `chi` 压缩中间件（单二进制不再有 Caddy 做 gzip），③静态处理拒绝点文件路径（`all:dist` 会把 `.gitignore` 嵌进二进制），④高德代理改为构造时预建、请求路径不再 `url.Parse`；⑤`infra/compose.yaml` 的对象存储端点由 `localhost:9000` 改为 `host.docker.internal:9000` 并加 `extra_hosts`（旧值在容器内指向容器自己，资产确认会失败）；⑥数据库排查提示补「密码特殊字符需 URL 编码」；⑦prod 与纯 http 的矛盾：保留 prod 严格校验，显式 `TRIPFOLIO_COOKIE_SECURE=false` 表示确认无 TLS，此时把 https 要求降为启动告警，并新增非 prod 的宽松模式提醒；⑧收尾：`docker/README` 与 `.env.example` 同步、`apps/client/README.md` 的 Caddy 表述修正（上一轮漏改）、HEALTHCHECK 只留镜像里的那份 | 未提交，由用户决定；迁移只前进不回退，回滚镜像前需确认新版迁移向后兼容 |
| 2026-09-16 | 部署兼容 | NAS 实测（PostgreSQL 13.23）在迁移 00009 报 `syntax error at or near "("`：该迁移用了 PostgreSQL 15+ 的列子集 `ON DELETE SET NULL (avatar_asset_id)`。改为 00009 建 `assets` 上的 BEFORE DELETE 触发器 `accounts_clear_avatar_reference`（只置空 `accounts.avatar_asset_id`、不动 `accounts.id`）+ 默认 NO ACTION 的复合外键，触发器缺失时删除会被外键拒绝、不留悬挂引用；函数体用 goose 的 `StatementBegin／End` 包裹以正确传递分号。按「文档先行」先改文档再改代码：数据库设计升 v0.5（基线改为 PostgreSQL 13+，外键、清理顺序、迁移顺序三处改写说明），`docker/README` 前置条件与排障表、单二进制部署设计 §5、技术选型的主数据库行与基线段落补版本下限，CLAUDE.md 索引同步为 v0.5。新增集成测试 `avatar_fk_test.go` 锁住两条 schema 契约。验证：本地 PG 18 上 `migrate up` 10 个全应用、`migrate down` 两次后触发器与函数计数归零；全量 Go 测试、gofmt、vet 通过；子代理在真实 PostgreSQL 13.23 上复验 10 个迁移全应用与 `migrate status` 全 applied、42 个包测试全绿（集成 10.5s）、触发器与复合外键语义实测正确；两条新断言各有反证（删触发器→用例 1 失败，外键换单列→用例 2 失败），证明断言不空转 | 未提交，由用户决定；资产目前只有软删语义、没有硬删路径，触发器仅由新增测试与手工 SQL 覆盖；NAS 侧需重新导出镜像，在原库上续跑迁移（1–8 已应用、9 未记录，无需重建库） |
| 2026-09-15 | T.12／S.15 追加 | 新增右下角固定悬浮的「看地图／看行程」按钮，主人侧行程详情页与访客分享页共用 `MapToggleFab.vue`：主人侧渲染为**真实链接**（不在图页签时指向 `trip-map`，在地图页签时指回 `trip-itinerary`，可中键／Ctrl 点击新标签打开），访客侧渲染为按钮并切换本地视图；两种形态都固定在视口右下角、按 `env(safe-area-inset-bottom)` 避开手机手势条，并在流内补一段空白避免盖住最后一屏内容。图标沿用 Lucide 20px 规范，新增 `map`／`list` 两个按需图标。按「文档先行」先补交互优化设计的地图与算路一节、分享设计 6.1 与详细计划 S.14 修订说明。验证：新增 `MapToggleFab.spec.ts`（3 项：两种状态的文案／图标与 toggle、链接形态与 Ctrl 点击、固定位置与安全区样式）与 `TripDetailPage.spec.ts`（2 项：页签切换时目标与文案反转、加载失败不显示），`SharePage.spec` 补悬浮按钮集成用例后全量前端测试、typecheck、build、Prettier 全绿；真实 Chromium 在 1440px 与 390px 下量测按钮均为距右 16px、距下 16px（移动端高 44px 命中 `--tf-control-size`），滚动 1600px 后位置不变，点击可在行程与地图之间往返切换 | 未提交，由用户决定；主人侧行程页的悬浮按钮只做了单元与页面集成测试，未在真实浏览器走查（需要登录，本机没有可用账号），移动壳下与访客页共用同一组件与样式；S.15 保持 `[~]` |
| 2026-09-16 | NAS 部署排障（非安全上下文） | 「获取验证码成功、点『注册并登录』报网络错误」的根因：注册要带 `client.device_id`，它来自 `deviceId()` → `crypto.randomUUID()`，而该 API **只在安全上下文（HTTPS 或 localhost）里存在**；`http://fff.pengit.top:61118` 是非安全上下文，调用抛 `TypeError`，且异常发生在 fetch 之前，所以页面只显示「网络错误，请稍后再试」、服务端日志里连这条请求都没有。用真实 Chromium 在非安全上下文来源上钉死了因果：`isSecureContext=false`、`crypto.randomUUID=undefined`、调用即 `TypeError: crypto.randomUUID is not a function`；同一台服务器跑完整注册流程，未修补时停在 `/register` 并显示该文案，仅给 `crypto.randomUUID` 打上 `getRandomValues` 补丁后跳到 `/trips`（服务端日志里 register 请求恰好 1 条，就是修补那次）。修复：新增 `apps/client/src/shared/randomId.ts`——有 `crypto.randomUUID` 就用它，缺失时用同样不受安全上下文限制的 `crypto.getRandomValues` 按 RFC 4122 自行拼 v4（不用 `Math.random`，两者都没有时抛出而不弱化随机）；替换 7 个文件共 9 处调用（安装标识、资料更新的 `Idempotency-Key`、写事务操作号、旅行/分类/行程项目的新建 id、资产上传四处）。同一类坑一并堵上：平台层剪贴板改为优先 `navigator.clipboard`、缺失时退回 `document.execCommand('copy')`，两者都不可用才抛错（调用方「复制失败，请手动选中链接复制」文案不变）。按「文档先行」先改文档：单二进制部署设计 §4 增「非安全上下文」小节（列出三个只在安全上下文存在的 API 与各自降级）、分享计划 S.12 Step 1 加修订说明。验证：新增 `randomId.spec.ts`（3 项）、`clipboard.spec.ts`（3 项）与 `auth.spec.ts` 的注册回归（1 项）共 11 项通过；把 `randomId()` 临时改回直接调 `crypto.randomUUID()`，这 3 条回归精确变红（同样报 `TypeError: crypto.randomUUID is not a function`），证明断言不空转；全量 53 文件 361 项前端测试、typecheck、build、Prettier 全绿；重新打包内嵌前端后用真实 Chromium 在 `http://172.28.128.1:8090` 复跑，不打补丁也落到 `/trips`，刷新页面后会话由 Cookie＋CSRF 正常恢复（`tripfolio_csrf` 在 http 下不带 `Secure`，符合设计），同来源 `navigator.clipboard` 确认为 `undefined` | 未提交，由用户决定；NAS 上要生效必须**重新构建并导入镜像**（前端已嵌进二进制，改前端就要重新打包）；`document.execCommand('copy')` 在无头 Chromium 里返回 false（没有剪贴板后端），该退路只能靠单元测试与真机手测确认，失败时提示手动复制；`crypto.getRandomValues` 也不存在的古董浏览器仍会抛错，不做弱随机兜底 |
| 2026-09-16 | 部署排障（后台任务启动） | NAS 上出现 `producer.StartWorkContext timed out after 10s`（退出码 5），而失败块给的是密钥/邮件那几条装配建议——与真实原因无关；重启后自愈说明是瞬时故障。三处改动：①**重试**——River 拉取队列设置的超时是它内部写死的 10 秒，这一步是对 `river_queue` 的 `INSERT ... ON CONFLICT DO UPDATE`，远端或跨网数据库抖动一下就会被挡住，现在启动任务消费默认重试 3 次（退避 2s、4s），中途收到退出信号立即停止重试；②**现场取证**——重试耗尽后先探测数据库（`Ping` 往返耗时 + `pg_stat_activity` 里前 3 个非 idle 或开着事务的会话，含 pid、状态、等待事件、事务时长与 SQL 头部），把结论写进失败块的「现场信息」，探测自身失败也绝不覆盖原始错误；③**按报错形状分流**——`lock timeout (55P03)` 说明确实被锁挡住（连接池设了 `lock_timeout=5s`，5 秒即返回，给出清理持锁会话的语句），满 10 秒的 `context deadline exceeded` 说明数据库这段没响应、多半是网络抖动；后台任务阶段不再复用装配阶段的 `servicesHints`。按「文档先行」先改单二进制部署设计 §6（拆成「重试」与「现场取证」两层）与 `docker/README` 排障表退出码 5 行。验证：新增 5 项单测（重试后成功、耗尽时原样返回 River 错误、取消后只试一次且不等待退避、提示含分流关键词、连接池不可用时探测仍给结论）全过；**端到端**用另一个会话 `BEGIN; SELECT name FROM river_queue WHERE name='default' FOR UPDATE; SELECT pg_sleep(100);` 真锁住该行后启动二进制，日志出现两次「任务消费启动失败，稍后重试」，失败块打印「数据库探测正常，往返耗时 1ms」与「另一会话 pid=17827 状态 active，等待 PgSleep，事务已开 30s，SQL：BEGIN; SELECT name FROM river_queue WHERE name='default' FOR UPDATE;…」，与真实原因吻合；`gofmt -l .` 无输出、`go vet ./...` 干净、带测试库 URL 的全量 Go 测试全绿（含集成 9.362s） | 未提交，由用户决定；本机数据库带 `lock_timeout=5s` 所以复现出的是 55P03，用户上报的是满 10 秒的 deadline，两种形状已分别给出排查路径；重试次数与退避目前是代码常量（3 次、2s/4s），没有做成环境变量 |
| 2026-09-16 | NAS 部署排障（密钥／来源／纯 http） | 按 NAS 上真实踩到的三个报错逐条收口，都从「报错看不懂」改成「报错自带修法」：①`密钥 k1 至少 32 字节，当前 0 字节`——用户写成 `TRIPFOLIO_KEYRING=<base64>=`，漏了 `k1=` 前缀导致 base64 末尾的填充 `=` 被当作分隔符；`ParseKeyring` 现在识别这两种写法（整串就是 base64、以及值是空的但 kid 本身是 base64）并给出「请写成 k1=…」的可照抄写法，缺编号、缺前缀、非法 base64 各自独立报错，新增 `TestKeyringReportsMissingKIDPrefix`；同时不再要求用户手工生成——部署包 `install.sh` 与 `scripts/docker-deploy-nas.sh` 在 `TRIPFOLIO_KEYRING` 留空时自动生成一次、备份 `.env` 后写回，并注明「只生成一次，换掉会让会话、游标与验证码失效」。②注册获取验证码报 `403 CSRF_FAILED 请求来源不被允许`——`checkOrigin` 的报错现在点明实际收到的 Origin、「不在允许列表内（TRIPFOLIO_CORS_ORIGINS）」以及「来源包含端口，非默认端口要写全」，新增 `cookies_test.go`（`TestCheckOrigin` 7 个来源用例 + `TestCheckOriginRejectionExplainsHowToFix` 断言提示四要素）。③`TRIPFOLIO_WEB_BASE_URL 在生产环境必须是 https 地址`——纯 http 是自托管常态，这个硬校验只会把人挡在门外：站点地址 http／https 都接受，刷新 Cookie 的 `Secure` 与 `__Host-` 前缀改为**跟随站点协议**自动决定（http 关闭、https 在 prod 默认开启），http 部署只打印一条代价告警，`TRIPFOLIO_COOKIE_SECURE` 降级为特殊场景的显式覆盖（与协议冲突时告警说明登录会失效），`prod` 不再需要任何额外开关。按「文档先行」先改文档再改代码：单二进制部署设计 §4／§5、`docker/README`（变量表、前置条件、排障表、「没有 HTTPS 时」）、`docker/.env.example`、`apps/server/.env.example`、`infra/README`、根 `README` 与已完成的分享计划 S.3 加修订说明。验证：`gofmt -l .` 无输出、`go vet ./...` 干净、带测试库 URL 的 `go test ./... -count=1` 42 个包全绿（集成 9.717s）、`pnpm typecheck` 通过、51 文件 354 项前端测试通过、`pnpm format:check` 通过（`docker/README` 表格由 Prettier 重新对齐） | 未提交，由用户决定；NAS 侧要做的三件事：把 `TRIPFOLIO_CORS_ORIGINS` 改成与实际访问地址完全一致（含 `:61118` 端口）、重新导出并上传镜像拿到这三处改动、`.env` 改动后必须 `docker compose up -d --force-recreate` 才会生效（`restart` 不重读）；OSS 与 SMTP 仍未填齐，文件授权接口会返回 503 |
| 2026-09-16 | 移动壳旅行页修复 | 窄屏（视口 ≤ 767px）与安卓进移动壳后只能看到「服务端可用」与「主题中心」，无任何可操作内容：`router/routes.ts` 里只有 `tripList` 一条把移动壳指向移动专属页，而 `mobile/pages/TripListPage.vue` 是个 40 行的自检占位页——只调 `metadata.load()`、不调 `listTrips`，没有列表也没有指向 `trip-detail` 的入口；其余路由（登录／注册／账号／回收站／旅行详情与五个页签）都按文档口径复用桌面实现，只有这一条是遗留。按「文档先行」先补 `docs/architecture/2026-09-14-高德地图与行程路线.md` 的前端与安全代理一节，再把该文件改成外层页面：内嵌桌面 `TripListPage`（路由无需改动，仍指向本文件），自身只保留平台自检入口；主题中心、回收站与账号按用户要求移到移动壳顶栏右上角，用 `IconAction` 的 Lucide 图标与桌面壳顶栏保持一致（账号在移动壳也做成图标，不带昵称文字，图标清单新增 `User`），顶栏左上角加返回：非根页面显示、优先 `router.back()`，没有可回退历史（新标签／分享／刷新后直接进入）时退回旅行列表，旅行列表是根页面故不显示。同步改 `docs/architecture/2026-09-13-前端主题体系设计.md` 的图标清单。新增 `mobile/MobileShell.spec.ts` 3 项（登录后三个图标入口与 href、未登录只留主题中心、根页面无返回且子页面返回落到旅行列表）与改写 `mobile/pages/TripListPage.spec.ts` 2 项（内嵌桌面列表、原生或 DEV 下自检入口可点击进入）。 | **未验证**：本会话 shell 无法执行（每次 `pwsh` 均以 `0xC0000142` DLL 初始化失败退出），`pnpm typecheck`／`pnpm test`／`pnpm build` 与浏览器走查都没跑，请在本机补跑；`MobileShell.spec.ts` 是首个真正挂载壳的用例，会连带加载 Element Plus 与 Vant 的整包 CSS（`vitest.config.ts` 里 `css: true`），若 jsdom 解析报错就在该用例里 mock 掉这几个 CSS 导入；移动壳旅行页会随桌面列表一起加载 Element Plus，与「移动包不含 Element Plus」的远期目标相悖，等安卓阶段用 Vant 重做移动页时一并收口 |
| 2026-09-16 | 底图与密码规则 | 按用户要求两项：①高德底图由 `amap://styles/whitesmoke`（远山黛）改为 `amap://styles/fresh`（草色青，暖绿底色，与「有机自然」主题同色系），只改 `AmapView.vue` 初始化选项一处；顺带核对了可选范围——官方内置共 10 个样式（normal／whitesmoke／light／fresh／grey／macaron／graffiti／dark／blue／darkblue），也可在控制台用自定义地图（GeoHUB）生成 `amap://styles/<样式ID>`，卫星图、路网、第三方瓦片属于图层而非样式；`blue`／`darkblue` 撞 CLAUDE.md 的「禁用蓝紫色」，未选。②密码长度下限由 10 位放宽到 8 位，用户选定「不限字符种类」（符号与中文仍可）。按「文档先行」先改 `docs/api/2026-09-11-P0接口设计.md` 的 3.1 与文首修订说明，再改契约与代码：`packages/contracts/openapi/v1/schemas/account.yaml` 三处 `minLength: 10 → 8`（注册 `password`、重置与改密的 `new_password`；登录与复验只有 `maxLength: 128`，保持不动），已打包入库的 `packages/contracts/dist/openapi.v1.yaml` 同步同样三处，后端 `ValidatePassword` 改为 8–128（唯一入口，注册／重置／改密三处调用），前端 `isValidPassword` 与注册、重置、改密三页的校验文案同步，改密页表单标签补上区间；上限 128 与「不强制某类字符组合」不变，登录与密码复验不校验下限。 | **未验证**：本会话 shell 无法执行（每次 `pwsh` 均以 `0xC0000142` DLL 初始化失败退出），`pnpm --filter @tripfolio/contracts build`、`gofmt`／`go vet`／`go test`、`pnpm typecheck`／`test`／`build`、`pnpm format:check` 与浏览器走查都没跑；**`dist/openapi.v1.yaml` 是手改的**（生成器在本会话跑不了），务必跑一次 contracts build 确认已打包产物与源一致、无生成物漂移；Go 生成物与 TS 类型不含 minLength，无需重新生成；既有账号的密码不受影响（登录只校验上限），此前下限是 10 所以不存在 8–9 位的老密码 |
| 2026-09-16 | 算路批量化为途经点 | 用户选定「驾车改用途经点一次算完」。根因见上一轮排查：相邻两站一次上游请求，前端与后端各有一道 1.1 秒节拍闸，N 站要 (N−1)×1.1 秒。改动按「文档先行」先写文档再写代码：`docs/api/2026-09-11-P0接口设计.md` 增 `GET /geo/trip-routes`（2.10 表、GeoTripRouteQuery、3.10 规则、文首补充，接口总数 96→97，CLAUDE.md 索引同步）、`docs/architecture/2026-09-14-高德地图与行程路线.md` 增批量算路一条。契约源：`schemas/geo.yaml` 增 `GeoRouteListResponse`、`paths/geo.yaml` 增 `trip-routes`（`points` 为 `经度,纬度;经度,纬度`，最多 50 点）、`openapi.yaml` 挂路径。服务端：`modules/geo` 的 `Provider` 增 `CalculateTripRoutes` 与 `Service.Routes`／`RoutesAs`（与 `Route`／`RouteAs` 同形，含 2–50 点校验）；`adapters/geo/routes.go` 抽出 `acquireRouteSlot`（门闩＋节拍＋限流，语义不变）与 `requestRoute`，新增 `CalculateTripRoutes`：驾车按高德途经点上限每 16 段（17 个坐标）分片、每片一次上游请求，返回的整条路线用「途经点在合并路径上的最近顶点」定分段点、再把每一步按起点下标归属到段，各段距离与时长是各步之和（高德口径，段与段首尾相接，各段之和等于总量），命中缓存的段不请求，某片失败只让该片退回逐段；上游缺步骤级明细（`step_distance`／`distance` 与 `cost.duration`／`duration` 两种写法都兼容，都没有时）返回 `errNoStepDetail` 整体退回逐段，不做估算。`transport/httpapi/geo.go` 增处理器与 `parseRoutePoints`（格式错误 422）。分享链路：`share/ports.go` 的 `RouteSource` 增 `RoutesAs`，`share/routes.go` 先试整趟批量、`hardUnavailable` 时整体 503、其余情况退回原逐段循环，保持「单段失败只计 failed_leg_count」语义。前端：`shared/api/geo.ts` 增 `calculateTripRoutes`，`useItineraryRoutes.ts` 在驾车且段数 ≥2 且坐标 ≤50 时先试一次批量（成功即整批置 ready 并写各段缓存），失败落回原逐段队列（保留 1.1 秒节拍、重试与错误分级）。新增测试：`adapters/geo/trip_routes_test.go` 三项（一次途经点请求算 4 点 3 段且各段之和等于总量、17 段按 16+1 分两片、上游无步骤明细时 1 次批量失败后逐段 3 次）、`modules/geo/service_test.go` 增 `Routes`／`RoutesAs` 的限流键与坐标数校验、`share/service_test.go` 的 `fakeRoutes` 补 `RoutesAs`（逐段委托，使既有逐段降级断言继续成立）。 | **未验证且需你补两件事**：①本会话 shell 无法执行（每次 `pwsh` 均以 `0xC0000142` DLL 初始化失败退出），生成器跑不了——`go generate ./internal/transport/httpapi/` 与 `pnpm --filter @tripfolio/contracts build` 必须在你这侧跑（我按生成的命名约定写了 `generated.CalculateTripRoutesRequestObject`／`CalculateTripRoutes200JSONResponse`／`CalculateTripRoutesParams.Points`，未手改任何生成物）；②契约打包文件 `dist/openapi.v1.yaml` 与 `generated/openapi.v1.d.ts`、`api.gen.go` 仍是旧内容，跑完生成器再一起看；此外 `gofmt`／`go vet`／全量 Go 测试、`pnpm typecheck`／`test`／`build` 都没跑，「步骤级字段名」是按两版文档的写法同时兼容的，真实响应若两处都没有会退回逐段（慢但数字准）。**另记两个未处理项**：`PackingLibraryDialog.vue` 第 31、37 行各有一个裸控制字符（应为空格），导致该文件被读取工具判为二进制而无法编辑，建议用 `(Get-Content -Raw $p) -replace "[\x00-\x08\x0e-\x1f]"," " \| Set-Content -NoNewline -Encoding utf8 $p` 清掉；用户报的「加入清单 crypto.randomUUID 报错」经核对是**线上跑的是旧包**（线上 `PackingTab-D0IPVQuL.js`，本仓库产物 `PackingTab-fG531oIv.js`，当前源码已无未加守卫的调用），重新构建并导入镜像即可，同一次部署会带上草色青底图、密码下限 8 与移动壳修复 |
